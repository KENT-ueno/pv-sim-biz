# 産業用PV需給シミュレーター プロジェクト指示書

## プロジェクト概要
産業用（高圧・特別高圧）太陽光発電＋蓄電池の需給シミュレーター。
家庭用版（pv-sim-gh）からフォークし、産業用に特化した機能に差替える。
URL: https://huggingface.co/spaces/hachinai/pv-sim-biz

**姉妹プロジェクト（家庭用）:** https://huggingface.co/spaces/hachinai/pv-sim-gh

## 技術スタック
- Python / Gradio / Plotly / pvlib / pandas / numpy
- PuLP + CBC（蓄電池充放電最適化）← 新規追加
- SQLite（radiation.db: 47都道府県のNEDO METPV-20気象データ）
- デプロイ先: Hugging Face Spaces

## ファイル構成
- `app.py` — スタンドアロン構成。家庭用版をベースに産業用に差替え
- `radiation.db` — 47地点の日射量・気温等10要素のSQLite DB（家庭用と共通）
- `requirements.txt` — pip依存パッケージ（PuLP追加）
- `README.md` — HF Spaces用メタデータ
- `comstock_*.csv` — 産業用需要プリセットCSV（6種、要作成）

## 家庭用から引き継ぐもの（共通基盤）
- JIS C 8907の発電量計算ロジック
- NEDO METPV-20の47地点日射量DB（radiation.db）
- pvlibによるGHI→POA変換（Erbsモデル、isotropicモデル）
- 30分×365日＝17,520コマのフレームワーク
- 最大8面アレイ、面ごとのPCS出力制限
- JIS C 8907温度補正（毎時気温反映）
- 蓄電池充放電シミュレーション基盤（SOC追跡）

## 家庭用から削除するもの
- 需要プリセット「電気＋ガス併用」「オール電化」
- 家庭用需要CSV（japan_residential_*.csv）
- 電気料金：従量電灯B（3段階制）
- 電気料金：スマートライフS/L（時間帯別）
- ガス料金計算（東京ガス一般料金）
- オール電化 vs 電気＋ガス併用の比較機能
- FIT売電（家庭用10kW未満の前期/後期/卒FIT）

---

## 2つのユースケース（モード切替で対応）

このシミュレーターは1つのスペース内で以下の2モードを提供する：

### モードA: 産業用自家消費型（リース/PPA営業ツール）
- **ペルソナ:** 太陽光・蓄電池の営業担当、EPC事業者
- **用途:** 需要家への提案書作成
- **出力:** 年間コスト削減額、投資回収年数、CO2削減量
- P-IRR（プロジェクトIRR）は出さない（営業側の収益構造を需要家に見せない）
- リース料・PPA単価をパラメータとして「需要家にとってのメリット」を提示
- 電気料金: 高圧・特高のデマンドチャージ

### モードB: マイクログリッド（MG事業検討ツール）
- **ペルソナ:** MG事業の企画担当、特定送配電事業の検討者
- **用途:** MG事業の投資判断
- **出力:** P-IRR（プロジェクトIRR）の算出がゴール
- 追加パラメータ: 網内売電単価、託送料金相当、設備投資額、運営コスト
- キャッシュフロー計算（年次ベース）
- 特定送配電事業としての電力販売収益

### 共通基盤（両モード共通）
- Phase 1の機能すべて（需要プリセット6種、複数施設合算、PV発電、蓄電池ルールベース）
- Phase 2のデマンド追跡、CO2削減量
- Phase 3の蓄電池最適化（PuLP/CBC）

### モード分岐のタイミング
- Phase 2の経済性計算で分岐する
- UIにモード選択（「自家消費型」/「マイクログリッド」）を追加
- モードAは需要家視点の経済メリットを表示
- モードBは事業者視点のP-IRR・キャッシュフローを表示

### マイクログリッドと産業用自家消費の計算上の違い
- **自家消費型:** PV余剰 → 系統に売電（FIT/FIP）
  - 経済メリット = 買電削減 + 売電収入
- **MG型:** PV余剰 → 網内需要家に売電（小売価格）→ 残余を系統売電
  - 経済メリット = 網内売電収入（系統買電単価との差額）+ 系統売電収入
- 計算ロジック上は自家消費の部分が「網内供給」に読み替わるだけで、min(generation, demand)の構造は同じ。異なるのは経済性計算の単価部分。

### Phase 2のステップへの影響
- Step 2-1〜2-3（高圧料金・デマンド追跡）は両モード共通で進めてよい
- Step 2-4以降でモード分岐のUI実装が必要
- MG向けのP-IRR計算はPhase 2の後半または別Phaseとして追加

---

## 開発フェーズ

### Phase 1: 産業用需要プリセット＋基本需給シミュレーション

#### 需要プリセット（NREL ComStock EULP ベース）
以下の6タイプをASHRAE 4A（東京相当）気候区分から取得・日本向け補正：

| # | ComStock分類 | 日本での想定用途 | 規模目安 |
|---|------------|-------------|--------|
| 1 | Medium Office | 役所・自治体庁舎 | 約4,500m², 3階建て |
| 2 | Primary School | 公立小学校 | 約6,900m², 1階建て |
| 3 | Secondary School | 公立中学校・高校 | 約19,600m², 2階建て |
| 4 | Hospital | 公立病院 | 約22,400m², 5階建て |
| 5 | Large Hotel | ホテル（東横・アパ規模） | 約11,300m², 6階建て |
| 6 | Retail | コンビニ・小売店 | 約2,300m², 1階建て |

#### 複数施設合算UI（マイクログリッド対応）
ユーザーが複数の施設タイプを組み合わせて1つの需要カーブを作成可能：
```
需要構成:
  [＋施設追加]
  施設1: [Medium Office ▼] × [1] 棟  年間消費量 [___] kWh/年
  施設2: [Primary School ▼] × [2] 棟  年間消費量 [___] kWh/年
  ─────────────
  合計需要: ○○○ kWh/年
```
- 各プリセットは30分×365日の時系列データ → 同じ時間軸で合算
- 棟数で倍率スケーリング、年間kWh入力で追加スケーリング
- カスタムCSVアップロード対応（30分デマンドデータを持つ需要家向け）

#### 自家消費計算
- 家庭用と同じmin/max演算（蓄電池なし時）
- 蓄電池あり時はルールベース充放電（Phase 1では家庭用と同じロジック）

### Phase 2: 高圧・特高の料金体系＋経済性計算

#### Step 2-1: 高圧電気料金 ✅
- **基本料金** = 基本料金単価（円/kW） × 契約電力（kW）× 力率割引
- **契約電力** = 過去12ヶ月の30分デマンド最大値（実量制）
- **電力量料金** = 使用量（kWh） × 電力量単価（円/kWh）
- **力率割引**: デフォルト85%（パラメータ化）
- 確認済みデフォルト値（東京電力EP高圧A: 基本1,890円/kW、夏季19.93円、他18.77円、再エネ4.18円）

#### Step 2-2: 特別高圧電気料金 ✅
- 契約種別ドロップダウン（高圧/特別高圧）で切替、単価自動反映
- 確認済みデフォルト値（東京電力EP 特別高圧: 基本1,770円/kW、夏季18.48円、他17.47円）
- 受電設備工事費: 27,500円/kVA × 契約電力（一時費用として表示）

#### Step 2-3: デマンド追跡 ✅
- 月別最大デマンド棒グラフ（導入前後比較）をタブ表示
- 契約電力ライン（破線）で可視化
- 計算結果テキストに月別最大kW一覧表を追加
- ※需要データ（ComStock）の精度が低く効果が見えづらい → 需要データ差替えで対応予定

#### Step 2-4: 産業用FIT売電 ✅
- 売電制度: FIT利用あり / FIT利用なし の2択
- FIT利用あり: 経過年数で自動単価切替（1-5年: 19円、6-20年: 8.3円、21年～: 8.5円）
- FIT利用なし: デフォルト8.50円（編集可能）
- 売電収入・総合経済メリットを計算結果に表示

#### Step 2-5: 逆潮流禁止 ✅ ＋ 補助金適用（未実装）
- **逆潮流禁止（RPR制約）** ✅: 売電モード「逆潮流禁止（売電なし）」で切替
  - 蓄電池充電優先 → 満充電時は出力抑制（カーテイルメント）
  - 出力抑制量を計算結果に表示
- **補助金適用**: 未実装 → Step 2-5bとして次回実装
  - PV・蓄電池導入補助金をパラメータ化
  - 補助金額（円）または補助率（%）を入力可能
  - 投資回収計算に反映
  - **実装時の確認事項:** 主要な補助金制度の名称・金額

#### Step 2-6: 産業用PV・蓄電池単価 ✅
- PVシステム単価: 158,000 円/kW（産業用デフォルト）
- 蓄電池単価: 200,000 円/kWh（産業用デフォルト）
- UIで自由に変更可能
- 補助金: PV・蓄電池それぞれ補助率（%）で設定可能、デフォルト0%
- 初期投資額・補助金控除後の実質投資額・単純投資回収年数を表示

#### Step 2-7: CO2削減量の算出 ✅
- 排出係数: 0.000431 t-CO2/kWh（全国平均デフォルト、手入力で変更可能）
- 系統購入削減量 × 排出係数 = CO2削減量（t-CO2/年）
- 計算結果に表示

### Phase 3: 蓄電池充放電の最適化（PuLP/CBC） ✅

#### 最適化の目的
蓄電池の30分ごとの充放電スケジュールを最適化し、年間トータルコスト（基本料金＋電力量料金 − 売電収入）を最小化する。

#### 定式化（線形計画問題）
- **決定変数**: 各30分コマの充電量・放電量（17,520コマ × 2変数）
- **目的関数**: 年間電気代の最小化
  - = Σ（系統購入量 × 電力量単価） + 基本料金単価 × max(30分デマンド) − Σ（売電量 × 売電単価）
- **制約条件**:
  - SOC範囲: SOC_min ≤ SOC(t) ≤ SOC_max
  - 充放電レート: 0 ≤ charge(t) ≤ max_charge × 0.5h
  - 0 ≤ discharge(t) ≤ max_discharge × 0.5h
  - SOC遷移: SOC(t+1) = SOC(t) + charge(t)×η − discharge(t)/η
  - エネルギーバランス: grid_import(t) + pv(t) + discharge(t) = demand(t) + charge(t) + export(t)
  - 非負制約: grid_import(t) ≥ 0, export(t) ≥ 0

#### ピークカット効果
- デマンド最大値の最小化 → 契約電力の削減 → 基本料金の削減
- max(30分デマンド)はlinearizeが必要（補助変数で線形化）

#### HF Spaces上での実行性能
- PuLP + CBCソルバーはHF Spacesで動作確認済み（家庭用プロジェクトで調査済み）
- 17,520コマの線形計画は数十秒で解ける見込み

### Phase 5: 蓄電池最適容量探索 ✅

#### 目的
蓄電池容量を変数として、P-IRRが最大となる最適容量を求める。
最適解を1つ提示するとともに、最適化の過程をグラフで可視化する。

#### 手法: 2段階アプローチ

##### 段階1: LP一体化で最適容量を高速に求める（約15秒）
- 蓄電池容量をPuLPの決定変数に含める
- SOC上限制約: SOC(t) ≤ capacity（capacityが変数）
- 目的関数: 年間運用コスト ＋ 蓄電池投資の年額換算コスト の最小化
  - 年額換算 = capacity × 蓄電池単価 / 回収年数（UIで設定済みの回収年数を使用）
  - これはP-IRR最大化と等価
- 1回のLP実行で最適容量と最適充放電スケジュールが同時に求まる
- 結果を即座にユーザーに表示

##### 段階2: グリッドサーチで過程グラフを生成（約2〜3分）
- 段階1で求めた最適容量の前後を含む範囲でグリッドサーチ
- 例: 最適容量が500kWhなら、0〜1500kWhを15〜20段階で探索
- 各容量で既存のPuLP充放電最適化を実行し以下を記録:
  - 年間コスト削減額（万円/年）
  - 投資額（万円）
  - P-IRR（%）
  - 投資回収年数（年）
  - 契約電力削減量（kW）
  - CO2削減量（t-CO2/年）

#### 出力グラフ（Plotly）
- 横軸: 蓄電池容量（kWh）
- プロットするカーブ:
  - 年間コスト削減額（万円/年）— 逓減カーブ
  - 投資額（万円）— 直線的に上昇
  - P-IRR（%）— 山型カーブ（ピークが最適容量）
  - 投資回収年数（年）— P-IRRと逆の関係
- 最適容量の点にマーカーまたは縦破線で「最適」を強調表示

#### 出力テキスト
現在の出力形式に追加する形で、最適容量の結果を表示:
```
最適蓄電池容量: ○○○ kWh
P-IRR: ○○.○%
契約電力: △△ kW → □□ kW（▲▲ kW 削減）
年間トータルコスト削減: ●●万円/年
CO2削減量: ●● t-CO2/年
投資回収見込み: ●● 年
```

#### 実装上の注意
- 段階1のLP一体化では、充放電レート上限（max_charge, max_discharge）はユーザー入力値を固定で使用
  - 産業用蓄電池はPCSの定格で決まるため固定が現実的
- 段階2のグリッドサーチは段階1+2を一括実行して結果表示（Gradio制約を考慮）
- ソルバーはCBCで実装（将来HiGHSへの切り替えも検討可能）

### Phase 4: モード分岐UI＋出力結果整形 ✅

#### Step 4-1: モード切替UI ✅
- マイクログリッドモード ON/OFF トグル（デフォルト: OFF＝自家消費型）
- ON時にMG専用入力欄を表示

#### Step 4-2: モードA（自家消費型）リース/PPA逆算＋需要家メリット ✅
- 事業モデル選択: 自己所有 / リース / PPA
- 事業者側入力:
  - 回収年数: デフォルト15年（変更可）
  - 目標P-IRR: デフォルト10%（変更可）
- 逆算ロジック:
  - 年間リース料 = 投資額 × r(1+r)^N / ((1+r)^N - 1)  （r=目標IRR, N=回収年数）
  - PPA単価 = 年間リース料 ÷ 年間自家消費量
- 需要家側出力:
  - 電気代削減額、リース料/PPA支払額、需要家メリット（15年間合計）
  - メリット→提案可能、デメリット→条件見直し
- P-IRRは需要家に見せない（事業者側の入力パラメータとして内部使用）

#### Step 4-3: モードB MG専用入力欄 ✅
- MG専用入力欄（モードON時のみ表示）:
  - 自営線距離: デフォルト2km（変更可）
  - 自営線単価: デフォルト3,000万円/km（変更可）
  - 年間運営コスト: イニシャルの2%（変更可）
  - P-IRR計算期間: デフォルト20年（変更可）
- 託送料金は想定せず、網内売電単価＝高圧料金（電力量料金）

#### Step 4-4: モードB MG収益計算＋P-IRR＋キャッシュフロー ✅
- MG収益モデル:
  - PV発電→網内売電: 電力量料金相当がまるまる収益（仕入れゼロ）
  - 系統買電→網内売電: 基本料金の差額のみが収益（電力量料金は相殺）
- P-IRR算出、年次キャッシュフロー表示

### Phase 6: 両面パネル＋積雪反射（野建て/発電シミュレーター用）
- pvlib.bifacialモジュールで裏面日射量計算
- NEDO METPV-20の積雪深データ（要素番号9）で動的アルベド切替（積雪時0.7、通常0.2）

---

## 開発の進め方

### Phase 1の実装ステップ（✅ 完了）

#### Step 0: 家庭用固有機能の削除 ✅
- 需要プリセット「電気＋ガス併用」「オール電化」を削除
- 家庭用料金計算（従量電灯、スマートライフ、ガス料金）を削除
- FIT家庭用（10kW未満）を削除
- 家庭用需要CSVの参照を削除
- UIタイトルを「産業用太陽光需給シミュレーター」に変更

#### Step 1: ComStockデータの取得・日本向け補正 ✅
- NREL ComStock EULP（ASHRAE 4A、6建物タイプ）をS3からダウンロード
- 15分→30分集約、m²あたり原単位に正規化
- 6種のプリセットCSV作成済み（comstock_*.csv、demand_kWh_per_m2列）
- ※需要データの精度は別途議論・差し替え予定

#### Step 2: 複数施設合算UIの実装 ✅
- 施設タイプ選択（6タイプ）× 延床面積 × 棟数のUI
- 施設数スライダー（1-6）で動的に行を表示
- 施設タイプ選択時にデフォルト延床面積を自動入力
- カスタムCSVアップロード（追加合算）対応
- 合算した需要カーブを計算に渡す

#### Step 3: 自家消費計算＋グラフの適応 ✅
- 合算需要カーブでの自家消費計算（家庭用ロジック流用）
- 月別グラフ（発電量 vs 需要量 vs 自家消費）
- 日別48コマグラフ（需給バランス＋SOC推移）
- 蓄電池シミュレーション（ルールベース）

#### Step 4: ローカルテスト ✅
- Firefox動作確認済み（2026-03-21）

### Phase 2の実装ステップ

#### 進め方ルール
- Step 2-1 から順番に1ステップずつ実装
- 各ステップの実装前に不足情報（価格、単価、係数など）をユーザーに確認
- ユーザーが忘れている可能性があるため、必ず都度質問する
- 各ステップ完了後、Firefoxでユーザーが動作確認

---

## コーディングルール
- **変更は最小限に:** 家庭用からの差分を明確にする
- **app.pyはスタンドアロン維持:** 需要CSVデータファイルの同梱は例外
- **日本語コメント:** 主要な関数・ロジックには日本語でコメントを付ける
- **Gradio互換性:** HF Spaces上のGradioバージョンとの互換性に注意
- **テスト:** 変更後は `python app.py` でローカル起動してエラーがないことを確認
- **PuLP追加時:** requirements.txtにPuLPを追加

## デプロイ手順
```bash
git add -A
git commit -m "Phase 1: 産業用需要プリセット＋基本需給シミュレーション"
git push
```
初回pushではHFのアクセストークンが求められる。
https://huggingface.co/settings/tokens でトークンを作成。

## 参考ドキュメント
- JIS C 8907:2005: https://kikakurui.com/c8/C8907-2005-01.html
- pvlib: https://pvlib-python.readthedocs.io/
- NEDO METPV-20: 日射量データベース
- NREL ComStock EULP: https://comstock.nrel.gov/page/datasets
- NREL OEDI S3: https://data.openei.org/s3_viewer?bucket=oedi-data-lake
- PuLP: https://coin-or.github.io/pulp/

## 未確定事項（要調査・要議論）
- [x] ComStockデータのダウンロード・日本向け補正方法の確定 → m²原単位CSV作成済み（精度は別途議論）
- [x] 高圧電気料金デフォルト値 → Step 2-1で確認済み（東京電力EP高圧A: 基本1,890円/kW、夏季19.93円、他18.77円、再エネ4.18円）
- [x] 特高の電気料金デフォルト値 → Step 2-2で確認済み（東京電力EP特別高圧: 基本1,770円/kW、夏季18.48円、他17.47円）
- [x] 産業用FIT売電単価 → Step 2-4で確認済み（FIT利用あり: 1-5年19円、6-20年8.3円、21年～8.5円 / FIT利用なし: 8.5円）
- [x] 産業用PVシステム単価・蓄電池単価 → Step 2-6で確認済み（PV: 158,000円/kW、蓄電池: 200,000円/kWh）
- [x] CO2排出係数のデフォルト値 → Step 2-7で確認済み（0.000431 t-CO2/kWh、全国平均）
- [x] 高圧→特高の受電設備工事費の目安 → Step 2-2で確認済み（27,500円/kVA）
- [x] 逆潮流禁止の詳細仕様 → Step 2-5で確認済み（蓄電池充電優先→出力抑制）
- [ ] 主要補助金制度の名称・金額 → Step 2-5bで確認
- [ ] 需要プリセットCSVの延床面積デフォルト値の見直し（コンビニ2,300m²は要検討）
