# 産業用PV需給シミュレーター プロジェクト指示書

## プロジェクト概要
産業用（高圧・特別高圧）太陽光発電＋蓄電池の需給シミュレーター。
家庭用版（pv-sim-gh）からフォークし、産業用に特化した機能に差替える。
URL: https://huggingface.co/spaces/hachinai/pv-sim-biz

**姉妹プロジェクト（家庭用）:** https://huggingface.co/spaces/hachinai/pv-sim-gh

## 技術スタック
- Python / Gradio / Plotly / pvlib / pandas / numpy
- PuLP + CBC（蓄電池充放電最適化）← 新規追加
- SQLite（radiation.db: **50地点**のNEDO METPV-20気象データ）
- デプロイ先: Hugging Face Spaces

## ファイル構成
- `app.py` — スタンドアロン構成。家庭用版をベースに産業用に差替え
- `radiation.db` — **50地点**の日射量・気温等10要素のSQLite DB（家庭用と共通）
- `requirements.txt` — pip依存パッケージ（PuLP追加）
- `README.md` — HF Spaces用メタデータ
- `comstock_*.csv` — 産業用需要プリセットCSV（6種、要作成）

## 家庭用から引き継ぐもの（共通基盤）
- JIS C 8907の発電量計算ロジック
- NEDO METPV-20の**50地点**日射量DB（radiation.db）
- pvlibによるGHI→POA変換（Erbsモデル、isotropicモデル）
- 30分×365日＝17,520コマのフレームワーク
- 最大8面アレイ、面ごとのPCS出力制限
- JIS C 8907温度補正（毎時気温反映）
- 蓄電池充放電シミュレーション基盤（SOC追跡）

## 家庭用から削除するもの
- 需要プリセット「電気＋ガス併用」「オール電化」
- 家庭用需要CSV（japan_residential_*.csv）
- 電気料金：従量電灯B（3段階制）
- 電気料金：スマートライフS/L（時間帯別）
- ガス料金計算（東京ガス一般料金）
- オール電化 vs 電気＋ガス併用の比較機能
- FIT売電（家庭用10kW未満の前期/後期/卒FIT）

---

## 2つのユースケース（モード切替で対応）

このシミュレーターは1つのスペース内で以下の2モードを提供する：

### モードA: 産業用自家消費型（リース/PPA営業ツール）
- **ペルソナ:** 太陽光・蓄電池の営業担当、EPC事業者
- **用途:** 需要家への提案書作成
- **出力:** 年間コスト削減額、投資回収年数、CO2削減量
- P-IRR（プロジェクトIRR）は出さない（営業側の収益構造を需要家に見せない）
- リース料・PPA単価をパラメータとして「需要家にとってのメリット」を提示
- 電気料金: 高圧・特高のデマンドチャージ

### モードB: マイクログリッド（MG事業検討ツール）
- **ペルソナ:** MG事業の企画担当、特定送配電事業の検討者
- **用途:** MG事業の投資判断
- **出力:** P-IRR（プロジェクトIRR）の算出がゴール
- 追加パラメータ: 網内売電単価、託送料金相当、設備投資額、運営コスト
- キャッシュフロー計算（年次ベース）
- 特定送配電事業としての電力販売収益

### 共通基盤（両モード共通）
- Phase 1の機能すべて（需要プリセット6種、複数施設合算、PV発電、蓄電池ルールベース）
- Phase 2のデマンド追跡、CO2削減量
- Phase 3の蓄電池最適化（PuLP/CBC）

### モード分岐のタイミング
- Phase 2の経済性計算で分岐する
- UIにモード選択（「自家消費型」/「マイクログリッド」）を追加
- モードAは需要家視点の経済メリットを表示
- モードBは事業者視点のP-IRR・キャッシュフローを表示

### マイクログリッドと産業用自家消費の計算上の違い
- **自家消費型:** PV余剰 → 系統に売電（FIT/FIP）
  - 経済メリット = 買電削減 + 売電収入
- **MG型:** PV余剰 → 網内需要家に売電（小売価格）→ 残余を系統売電
  - 経済メリット = 網内売電収入（系統買電単価との差額）+ 系統売電収入
- 計算ロジック上は自家消費の部分が「網内供給」に読み替わるだけで、min(generation, demand)の構造は同じ。異なるのは経済性計算の単価部分。

### Phase 2のステップへの影響
- Step 2-1〜2-3（高圧料金・デマンド追跡）は両モード共通で進めてよい
- Step 2-4以降でモード分岐のUI実装が必要
- MG向けのP-IRR計算はPhase 2の後半または別Phaseとして追加

---

## 開発フェーズ

### Phase 1: 産業用需要プリセット＋基本需給シミュレーション

#### 需要プリセット（NREL ComStock EULP ベース）
以下の6タイプをASHRAE 4A（東京相当）気候区分から取得・日本向け補正：

| # | ComStock分類 | 日本での想定用途 | 規模目安 |
|---|------------|-------------|--------|
| 1 | Medium Office | 役所・自治体庁舎 | 約4,500m², 3階建て |
| 2 | Primary School | 公立小学校 | 約6,900m², 1階建て |
| 3 | Secondary School | 公立中学校・高校 | 約19,600m², 2階建て |
| 4 | Hospital | 公立病院 | 約22,400m², 5階建て |
| 5 | Large Hotel | ホテル（東横・アパ規模） | 約11,300m², 6階建て |
| 6 | Retail | コンビニ・小売店 | 約2,300m², 1階建て |

#### 複数施設合算UI（マイクログリッド対応）
ユーザーが複数の施設タイプを組み合わせて1つの需要カーブを作成可能：
```
需要構成:
  [＋施設追加]
  施設1: [Medium Office ▼] × [1] 棟  年間消費量 [___] kWh/年
  施設2: [Primary School ▼] × [2] 棟  年間消費量 [___] kWh/年
  ─────────────
  合計需要: ○○○ kWh/年
```
- 各プリセットは30分×365日の時系列データ → 同じ時間軸で合算
- 棟数で倍率スケーリング、年間kWh入力で追加スケーリング
- カスタムCSVアップロード対応（30分デマンドデータを持つ需要家向け）

#### 自家消費計算
- 家庭用と同じmin/max演算（蓄電池なし時）
- 蓄電池あり時はルールベース充放電（Phase 1では家庭用と同じロジック）

### Phase 2: 高圧・特高の料金体系＋経済性計算

#### Step 2-1: 高圧電気料金 ✅
- **基本料金** = 基本料金単価（円/kW） × 契約電力（kW）× 力率割引
- **契約電力** = 過去12ヶ月の30分デマンド最大値（実量制）
- **電力量料金** = 使用量（kWh） × 電力量単価（円/kWh）
- **力率割引**: デフォルト85%（パラメータ化）
- 確認済みデフォルト値（東京電力EP高圧A: 基本1,890円/kW、夏季19.93円、他18.77円、再エネ4.18円）

#### Step 2-2: 特別高圧電気料金 ✅
- 契約種別ドロップダウン（高圧/特別高圧）で切替、単価自動反映
- 確認済みデフォルト値（東京電力EP 特別高圧: 基本1,770円/kW、夏季18.48円、他17.47円）
- 受電設備工事費: 27,500円/kVA × 契約電力（一時費用として表示）

#### Step 2-3: デマンド追跡 ✅
- 月別最大デマンド棒グラフ（導入前後比較）をタブ表示
- 契約電力ライン（破線）で可視化
- 計算結果テキストに月別最大kW一覧表を追加
- ※需要データ（ComStock）の精度が低く効果が見えづらい → 需要データ差替えで対応予定

#### Step 2-4: 産業用FIT売電 ✅
- 売電制度: FIT利用あり / FIT利用なし の2択
- FIT利用あり: 経過年数で自動単価切替（1-5年: 19円、6-20年: 8.3円、21年～: 8.5円）
- FIT利用なし: デフォルト8.50円（編集可能）
- 売電収入・総合経済メリットを計算結果に表示

#### Step 2-5: 逆潮流禁止 ✅ ＋ 補助金適用（未実装）
- **逆潮流禁止（RPR制約）** ✅: 売電モード「逆潮流禁止（売電なし）」で切替
  - 蓄電池充電優先 → 満充電時は出力抑制（カーテイルメント）
  - 出力抑制量を計算結果に表示
- **補助金適用**: 未実装 → Step 2-5bとして次回実装
  - PV・蓄電池導入補助金をパラメータ化
  - 補助金額（円）または補助率（%）を入力可能
  - 投資回収計算に反映
  - **実装時の確認事項:** 主要な補助金制度の名称・金額

#### Step 2-6: 産業用PV・蓄電池単価 ✅
- PVシステム単価: 158,000 円/kW（産業用デフォルト）
- 蓄電池単価: 200,000 円/kWh（産業用デフォルト）
- 特別高圧工事費: 27,500 円/kVA（高圧→特高変更時のみイニシャルに加算、手入力で変更可能）
- UIで自由に変更可能
- 補助金: PV・蓄電池それぞれ補助率（%）で設定可能、デフォルト0%
- 初期投資額・補助金控除後の実質投資額・単純投資回収年数を表示

#### Step 2-7: CO2削減量の算出 ✅
- 排出係数: 0.000431 t-CO2/kWh（全国平均デフォルト、手入力で変更可能）
- 系統購入削減量 × 排出係数 = CO2削減量（t-CO2/年）
- 計算結果に表示

### Phase 3: 蓄電池充放電の最適化（PuLP/CBC） ✅

#### 最適化の目的
蓄電池の30分ごとの充放電スケジュールを最適化し、年間トータルコスト（基本料金＋電力量料金 − 売電収入）を最小化する。

#### 定式化（線形計画問題）
- **決定変数**: 各30分コマの充電量・放電量（17,520コマ × 2変数）
- **目的関数**: 年間電気代の最小化
  - = Σ（系統購入量 × 電力量単価） + 基本料金単価 × max(30分デマンド) − Σ（売電量 × 売電単価）
- **制約条件**:
  - SOC範囲: SOC_min ≤ SOC(t) ≤ SOC_max
  - 充放電レート: 0 ≤ charge(t) ≤ max_charge × 0.5h
  - 0 ≤ discharge(t) ≤ max_discharge × 0.5h
  - SOC遷移: SOC(t+1) = SOC(t) + charge(t)×η − discharge(t)/η
  - エネルギーバランス: grid_import(t) + pv(t) + discharge(t) = demand(t) + charge(t) + export(t) + curtailment(t)
  - 出力抑制: curtailment(t) ≥ 0（逆潮流禁止時にPV余剰を吸収）
  - 終端SOC: SOC(T-1) = SOC(0)（年次比較の公平性）
  - 非負制約: grid_import(t) ≥ 0, export(t) ≥ 0

#### ピークカット効果
- デマンド最大値の最小化 → 契約電力の削減 → 基本料金の削減
- max(30分デマンド)はlinearizeが必要（補助変数で線形化）

#### HF Spaces上での実行性能
- PuLP + CBCソルバーはHF Spacesで動作確認済み（家庭用プロジェクトで調査済み）
- 17,520コマの線形計画は数十秒で解ける見込み

### Phase 5: 蓄電池最適容量探索 ✅

#### 目的
蓄電池容量を変数として、P-IRRが最大となる最適容量を求める。
最適解を1つ提示するとともに、最適化の過程をグラフで可視化する。

#### 手法: 2段階アプローチ

##### 段階1: LP一体化で最適容量を高速に求める（約15秒）
- 蓄電池容量をPuLPの決定変数に含める
- SOC上限制約: SOC(t) ≤ capacity（capacityが変数）
- 目的関数: 年間運用コスト ＋ 蓄電池投資の年額換算コスト の最小化
  - 年額換算 = capacity × 蓄電池単価 / 回収年数（UIで設定済みの回収年数を使用）
  - これはP-IRR最大化と等価
- 1回のLP実行で最適容量と最適充放電スケジュールが同時に求まる
- 結果を即座にユーザーに表示

##### 段階2: グリッドサーチで過程グラフを生成（約2〜3分）
- 段階1で求めた最適容量の前後を含む範囲でグリッドサーチ
- 例: 最適容量が500kWhなら、0〜1500kWhを15〜20段階で探索
- 各容量で既存のPuLP充放電最適化を実行し以下を記録:
  - 年間コスト削減額（万円/年）
  - 投資額（万円）
  - P-IRR（%）
  - 投資回収年数（年）
  - 契約電力削減量（kW）
  - CO2削減量（t-CO2/年）

#### 出力グラフ（Plotly）
- 横軸: 蓄電池容量（kWh）
- プロットするカーブ:
  - 年間コスト削減額（万円/年）— 逓減カーブ
  - 投資額（万円）— 直線的に上昇
  - P-IRR（%）— 山型カーブ（ピークが最適容量）
  - 投資回収年数（年）— P-IRRと逆の関係
- 最適容量の点にマーカーまたは縦破線で「最適」を強調表示

#### 出力テキスト
現在の出力形式に追加する形で、最適容量の結果を表示:
```
最適蓄電池容量: ○○○ kWh
P-IRR: ○○.○%
契約電力: △△ kW → □□ kW（▲▲ kW 削減）
年間トータルコスト削減: ●●万円/年
CO2削減量: ●● t-CO2/年
投資回収見込み: ●● 年
```

#### 実装上の注意
- 段階1のLP一体化では、充放電レート上限（max_charge, max_discharge）はユーザー入力値を固定で使用
  - 産業用蓄電池はPCSの定格で決まるため固定が現実的
- 蓄電池コストは補助金反映後の実質単価でLP・グリッドサーチに渡す
- LP定式化にカーテイルメント変数を含め、逆潮流禁止時もInfeasibleにならない
- 数値入力のデフォルト判定は `is not None` で統一（0入力を許容）
- 段階2のグリッドサーチは段階1+2を一括実行して結果表示（Gradio制約を考慮）
- ソルバーはCBCで実装（将来HiGHSへの切り替えも検討可能）

### Phase 4: モード分岐UI＋出力結果整形 ✅

#### Step 4-1: モード切替UI ✅
- マイクログリッドモード ON/OFF トグル（デフォルト: OFF＝自家消費型）
- ON時にMG専用入力欄を表示

#### Step 4-2: モードA（自家消費型）リース/PPA逆算＋需要家メリット ✅
- 事業モデル選択: 自己所有 / リース / PPA
- 事業者側入力:
  - 回収年数: デフォルト15年（変更可）
  - 目標P-IRR: デフォルト10%（変更可）
- 逆算ロジック:
  - 年間リース料 = 投資額 × r(1+r)^N / ((1+r)^N - 1)  （r=目標IRR, N=回収年数）
  - PPA単価 = 年間リース料 ÷ 年間自家消費量
  - **MG有効時**: 投資ベース = MG全額（PV+蓄電池+自営線）、運営コストもPPA単価/リース料に織込み
  - **MG無効時**: 投資ベース = PV+蓄電池の実質投資額（従来通り）
- 需要家側出力:
  - 電気代削減額、リース料/PPA支払額、需要家メリット（契約年数合計）
  - メリット→提案可能、デメリット→条件見直し
- P-IRRは需要家に見せない（事業者側の入力パラメータとして内部使用）

#### Step 4-3: モードB MG専用入力欄 ✅
- MG専用入力欄（モードON時のみ表示）:
  - 自営線距離: デフォルト2km（変更可）
  - 自営線単価: デフォルト3,000万円/km（変更可）
  - 年間運営コスト: イニシャルの2%（変更可）
  - P-IRR計算期間: デフォルト20年（変更可）

#### Step 4-4: モードB MG収益計算＋P-IRR＋キャッシュフロー ✅
- MG収益モデル:
  - PV発電→網内売電: PPA単価（PPA選択時）または電力量単価（その他）で計算
  - 基本料金差額: 個別施設の契約電力合計 vs MG合算後の契約電力で束ねメリットを計算
  - load_combined_demandが個別施設の需要データも保持（束ねメリット計算用）
  - 運営コストはPPA単価に織込み済のため、MG P-IRR ≒ 目標P-IRR + 束ねメリット分
- P-IRR算出、年次キャッシュフロー表示

### Phase 6: 両面パネル＋積雪反射 ✅
- pvlib.bifacial.infinite_shedsモジュールで裏面日射量計算
- NEDO METPV-20の積雪深データ（要素番号9）で動的アルベド切替（積雪時0.7、通常0.2）
- 両面パネルON/OFFチェックボックス（デフォルトOFF、片面動作に影響なし）
- パラメータ: bifaciality(0.75), GCR(0.4), 地上高(2.0m), pitch(5.0m)
- solarfactorsパッケージ追加（requirements.txt）
- pv-sim-genから移植、compute_poa_30minに両面分岐を追加

---

## Phase 7: データセンターモード（進行中・2026-09〜）

**本プロジェクトに「データセンター」を需要タイプとして統合する。**
元は独立プロジェクト `pv-sim-dc`（E:\jupyterlab\DCCN）として開発していたが、
HFの仕様変更で無料アカウントではGradio Spaceを新規作成できなくなったこと、
PUEを一定値に簡略化してUI上の差異が縮小したことから、本プロジェクトへ統合する方針に変更した。

### 設計の正典は docs/ にある

**作業前に必ず読むこと。** 本ファイルはその要約のみを持つ。

| ドキュメント | 内容 |
|---|---|
| `docs/design_spec.md` | **設計の正典**。モデル定式化・パラメータ・データ出典・未確定事項 |
| `docs/decision_log.md` | **判断の変遷**。なぜ独立Spaceをやめたか、案A〜Eの比較と却下理由 |
| `docs/dc_load_data_sources.md` | DC負荷データ6候補の調査と一次資料での検証記録 |
| `docs/hokkaido_power_voltage_tariff.md` | 北海道電力の受電電圧・タリフの一次ソース |

### 需要モデルの核心（設計書 §5-7）

```
demand(t) = IT定格容量 × IT負荷率(年平均) × 形状(t) × PUE × 0.5   [kWh/30分]
                          └ 水準:サイト固有   └ 形状:出典あり（年平均=1.0に正規化）
```

**水準と形状を分離する**のが要点。引用できる部分とユーザーが決める部分を混ぜない。
全プロファイルで形状の年平均が1.0なので、切り替えても年間電力量は変わらない。

- 容量指定は3方式（規模プリセット／IT容量直接／ラック数×density）。
  **延床面積は採らない**（DCはラック密度で電力密度が1桁変わるため、ComStock方式が転用できない）
- 負荷形状: CEC実測形状（米国商用DC、576点テーブル内蔵）／定常（AI学習）／日変動（自社サーバー室）
- PUEは一定値（ユーザー入力、既定1.40）。気温連動PUEは保留（出典が固まっていないため）

### 実装状況

| 段階 | 内容 | 状態 |
|---|---|---|
| 1 | 計算層の移植（タリフ表・DC需要モデルの定数と関数） | ✅ 完了（commit e783cf9） |
| 2 | `run_simulation` に需要ソース分岐（産業用 / データセンター） | ✅ 完了。`demand_source` / `dc_args` をキーワード引数の末尾に追加（省略時=産業用で従来どおり）。DC入力の解決と検証は `resolve_dc_demand()`、結果テキストは `format_dc_summary()`。PUEは一定値のみ |
| 3 | UI: 需要設定を `gr.Tabs()` で分割。共通セクションは据え置き | ✅ 完了。タブ「産業用・MG」/「データセンター」。選択タブは `gr.Tab.select` → `gr.State`、`on_click` が末尾の引数から `dc_args` を復元（並びは `DC_INPUT_KEYS`）。出力4タブは共通 |
| 4 | MCPツール `estimate_dc_demand` / `validate_dc_params` / `simulate_dc` | ✅ 完了・**本番反映済み**（2026-09-19）。`mcp_tools.py` に3ツール追加（計7ツール）。需要は `app.resolve_dc_demand()`、以降は産業用と同じ `_run_industrial_simulation`（`demand_override` / `grid_cap_kw` 引数）を再利用。受電上限を守れないときは `error` でなく `grid_cap_infeasible: true` の診断を返す。`estimate_pue` は気温連動PUEが保留のため作らない。MGは非対応。詳細は設計書 §10 実装メモ |
| 5 | **受電上限制約**: `optimize_battery()` に `grid_import_cap_kw` を追加（MGにも有用） | ✅ 完了・**本番反映済み**（2026-09-19）。DCタブに「系統受電上限」（制限なし／高圧に収める1,999kW／22・33kVに収める9,999kW／手入力）。上限を強制できるのは蓄電池「最適充放電（LP）」のみ（他は超過判定と必要量の目安の表示）。守れないときは `diagnose_grid_cap()` の診断（energy/power/capacity）を返す。上限ありのLPだけ周期SOC。上限なしのLPはビット同一。詳細は設計書 §7 実装メモ |
| （保留）| 需要側の調整（IT負荷のシフト）。DCCN Phase 5・将来項目で簡易版の範囲外 | **保留**（ユーザー判断 2026-09-19「ここをいじり始めるとよくない」）。再開時は蓄電池優先で「必要な柔軟負荷割合」を出力として返す形を先に検討し、実装前に目的を確認する。経緯は `docs/decision_log.md` 第8段階 |

### 作業上の厳守事項

- **既存の産業用・MGの機能は一切削除しない。** DCは需要タイプの追加であって置き換えではない
- **移植した計算層は追加のみ**で既存コードパスに触れていない。この性質を保つこと
- **変更後は必ず既存テストを全部回す**（回帰の有無を確認）:
  `test_fable5_fixes.py`（PASS 24）／`test_mcp_tools.py`（PASS 45）／`test_verify_battery_bug.py`／
  `test_dc_mode.py`（PASS 48。DC需要・分岐・UI配線）／`test_azimuth_input.py`（PASS 19。方位角欄の解釈）／
  `test_grid_cap.py`（PASS 75。受電上限・診断・LP。診断の健全性は乱数60ケースで診断⇔LPの実行可否が完全一致）／
  `test_mcp_dc_tools.py`（PASS 117。DCのMCPツール。UI（run_simulation）との数値一致・受電上限の各状態・入力検証）
- **産業用の出力を厳密に守るには**、変更前後で `run_simulation` の出力（結果テキスト・デバッグ・グラフJSON）を
  複数シナリオで保存→照合する（LPも決定論的に再現する）。段階2・3では5シナリオ×7項目=35項目が全て同一だった
- **push はユーザーの明示的指示を待つこと**（`origin` = HF Space なので **`main` への push = 本番デプロイ**）。
  DC統合は 2026-09-19 に `main` へマージ済み（`main` = 4d67237）。以後の開発ブランチは
  `feature/datacenter`（`main` と同じ位置から続ける）。**HF・GitHubにリモートの feature ブランチは無い**
  （公開済みだった非公開メモの除去のため削除した。下の「本番反映の記録」参照）

### DCで判明している重要な事実（設計書 §11・§5-7）

1. **需要が平坦かつ料金が年間単一単価だと蓄電池の価値はゼロになる**（バグではない）。
   契約電力が全コマ同値になりピークカット不能、年間単一単価なので時間裁定も不成立。
   LPは正しく charge=discharge=0 を返す
2. **CEC実測形状を入れるとPVが契約電力を削減できるようになる**（日中14-15時にピークがあるため）。
   実測: 札幌・中規模5,000kW・PV5,000kW で 6,417.9 → 6,285.3 kW
3. **商用DCの日内変動は極めて小さい**（CEC実測で91.0〜95.7%、振れ幅4.7ポイント）。
   日変動モードの既定（ピーク90%/ボトム50%）は商用DCには深すぎるため「自社サーバー室」用途に限定

### DCモードの現状の制約（段階2・3時点）

- **電気料金は共通の「電気料金設定」（既定=東京電力EP）を使う。** 段階1で移植した北海道電力タリフ表
  （`TARIFFS` / `get_tariff()`）はUIに接続しない方針（手入力で足りるため。ユーザー判断 2026-09-19）。DC結果には受電電圧の目安
  （ピーク2,000kW以上なら特別高圧）と契約種別の食い違い警告だけを出している
- 平坦な需要では、受電上限を指定しない限り蓄電池の価値はゼロになる（上記事実1）。受電上限（段階5）を指定すると、
  蓄電池が「上限を守る手段」として働く。ただし上限が低すぎる（基底負荷が上限に近い）と蓄電池では解決できない
  （診断のenergy条件。この場合は上限を上げる／PVを増やす／IT負荷を下げるが必要。需要シフトは保留中）
- 受電上限が厳しいとLPが遅くなる（CBC）。充電レートを極端に絞ると1回約37秒（時間制限120秒）。診断で確実に守れない
  ものは即座に返すのでLPは走らない

### 修正済み: UIの「方位角」欄が未入力で0°（北向き）になっていた既存バグ（commit 841ca22）

- **症状**: `gr.Number(value=None)` は Gradio 6.26 で「0」表示・**未操作でも0を送信**する（`precision`やstepの有無に
  よらない。最小アプリで実測）。このため「空欄なら方位ドロップダウン（南=180°）を使う」設計が働かず、既定の面1が
  北向き（方位角0°）で計算されていた。札幌・PV 1kWあたり年間発電量は南向き約1,008kWhに対し約682kWh。
  デバッグ欄に「南(0.0°)」と出るのが目印だった。開発当初「Gradioの仕様で初期値空欄が無理」とされていたのはこれ
- **修正**: 方位角欄を `gr.Textbox`（空欄=未指定）に変更。`run_simulation` が空欄/空白→方位（選択）、数値→直接入力
  （優先）、数値以外・nan/inf→日本語のエラー、明示的な `0` →真北、と解釈する。**未操作の既定は南向き（180°）になり、
  既定入力の発電量が約+48%になる（従来の値が過小だった）**。`test_azimuth_input.py`（PASS 19）で検証
- MCP経由（`faces` の `azimuth_deg` 既定180）は元から影響を受けない
- **本番反映済み（2026-09-19）**: `main` に cherry-pick（b9378b9）して push（`main` = 2f30079）。本番UIで未操作の既定が
  南(180.0°)・年間5,038.5kWh になることと、MCP経由の既知値（東京500kW・福岡MG）が不変なことを確認。
  （その後、DC統合を `main` にマージした。同一内容の別コミット 841ca22 と b9378b9 は競合しなかった）

### 本番反映の記録（2026-09-19、`main` = 4d67237）

- `main` に `feature/datacenter` をマージして HF・GitHub の `main` に push。本番の確認:
  産業用MCP（既定・LP+逆潮流禁止）の出力がローカルと**完全一致**、DCツール（`estimate_dc_demand` /
  `validate_dc_params` / `simulate_dc`）の結果もローカルと一致、公開スキーマは7ツール、UIに「データセンター」タブと
  「系統受電上限」あり（方位角欄は空欄の既定＝南向き）。受電上限＋LPは約16秒
- **非公開の構想メモ（`next_development_idea.md`）**: 以前の feature ブランチのpushでHF・GitHubに公開されていた。
  履歴から除去（13コミットを書き換え）して `main` に反映し、リモートの feature ブランチを削除した。
  ローカルには残し `.git/info/exclude` で無視している。設計書・判断記録の言及は「将来構想（非公開メモ）」に置換済み。
  ⚠ 旧コミットは、SHAを指定したURL（旧feature先端 9e0930d）ではHF・GitHubとも**まだ取得できる**（ブランチ削除では消えない）。
  完全に消すには GitHub のサポート依頼／HFの履歴スカッシュ等が必要（未実施）
- ローカルに `backup/feature-datacenter-before-memo-purge`（メモ入りの旧履歴）がある。**pushしないこと**（不要なら削除）

---

## 開発の進め方

### Phase 1の実装ステップ（✅ 完了）

#### Step 0: 家庭用固有機能の削除 ✅
- 需要プリセット「電気＋ガス併用」「オール電化」を削除
- 家庭用料金計算（従量電灯、スマートライフ、ガス料金）を削除
- FIT家庭用（10kW未満）を削除
- 家庭用需要CSVの参照を削除
- UIタイトルを「産業用太陽光需給シミュレーター」に変更

#### Step 1: ComStockデータの取得・日本向け補正 ✅
- NREL ComStock EULP（ASHRAE 4A、6建物タイプ）をS3からダウンロード
- 15分→30分集約、m²あたり原単位に正規化
- 6種のプリセットCSV作成済み（comstock_*.csv、demand_kWh_per_m2列）
- ※需要データの精度は別途議論・差し替え予定

#### Step 2: 複数施設合算UIの実装 ✅
- 施設タイプ選択（6タイプ）× 延床面積 × 棟数のUI
- 施設数スライダー（1-6）で動的に行を表示
- 施設タイプ選択時にデフォルト延床面積を自動入力
- カスタムCSVアップロード（追加合算）対応
- 合算した需要カーブを計算に渡す

#### Step 3: 自家消費計算＋グラフの適応 ✅
- 合算需要カーブでの自家消費計算（家庭用ロジック流用）
- 月別グラフ（発電量 vs 需要量 vs 自家消費）
- 日別48コマグラフ（需給バランス＋SOC推移）
- 蓄電池シミュレーション（ルールベース）

#### Step 4: ローカルテスト ✅
- Firefox動作確認済み（2026-03-21）

### Phase 2の実装ステップ

#### 進め方ルール
- Step 2-1 から順番に1ステップずつ実装
- 各ステップの実装前に不足情報（価格、単価、係数など）をユーザーに確認
- ユーザーが忘れている可能性があるため、必ず都度質問する
- 各ステップ完了後、Firefoxでユーザーが動作確認

---

## コーディングルール
- **変更は最小限に:** 家庭用からの差分を明確にする
- **app.pyはスタンドアロン維持:** 需要CSVデータファイルの同梱は例外
- **日本語コメント:** 主要な関数・ロジックには日本語でコメントを付ける
- **Gradio互換性:** HF Spaces上のGradioバージョンとの互換性に注意
- **テスト:** 変更後は `python app.py` でローカル起動してエラーがないことを確認
- **PuLP追加時:** requirements.txtにPuLPを追加

## デプロイ手順
```bash
git add -A
git commit -m "Phase 1: 産業用需要プリセット＋基本需給シミュレーション"
git push
```
初回pushではHFのアクセストークンが求められる。
https://huggingface.co/settings/tokens でトークンを作成。

## 参考ドキュメント
- JIS C 8907:2005: https://kikakurui.com/c8/C8907-2005-01.html
- pvlib: https://pvlib-python.readthedocs.io/
- NEDO METPV-20: 日射量データベース
- NREL ComStock EULP: https://comstock.nrel.gov/page/datasets
- NREL OEDI S3: https://data.openei.org/s3_viewer?bucket=oedi-data-lake
- PuLP: https://coin-or.github.io/pulp/

## 未確定事項（要調査・要議論）
- [x] ComStockデータのダウンロード・日本向け補正方法の確定 → m²原単位CSV作成済み（精度は別途議論）
- [x] 高圧電気料金デフォルト値 → Step 2-1で確認済み（東京電力EP高圧A: 基本1,890円/kW、夏季19.93円、他18.77円、再エネ4.18円）
- [x] 特高の電気料金デフォルト値 → Step 2-2で確認済み（東京電力EP特別高圧: 基本1,770円/kW、夏季18.48円、他17.47円）
- [x] 産業用FIT売電単価 → Step 2-4で確認済み（FIT利用あり: 1-5年19円、6-20年8.3円、21年～8.5円 / FIT利用なし: 8.5円）
- [x] 産業用PVシステム単価・蓄電池単価 → Step 2-6で確認済み（PV: 158,000円/kW、蓄電池: 200,000円/kWh）
- [x] CO2排出係数のデフォルト値 → Step 2-7で確認済み（0.000431 t-CO2/kWh、全国平均）
- [x] 高圧→特高の受電設備工事費の目安 → Step 2-2で確認済み（27,500円/kVA）
  - ⚠ 2026-09-19 ユーザー指摘: この単価は**高圧の受電設備の単価で、特高ではない可能性が高い**（出典の記録なし）。
    特高・22・33kV→66kV の単価は手元にない。値は変えていない（手入力で変更可）。DCの「受電増強 vs PV/蓄電池」の
    比較機能は、単価が確定しないため作らない（docs/decision_log.md 第10段階）
- [x] 逆潮流禁止の詳細仕様 → Step 2-5で確認済み（蓄電池充電優先→出力抑制）
- [ ] 主要補助金制度の名称・金額 → Step 2-5bで確認
- [ ] 需要プリセットCSVの延床面積デフォルト値の見直し（コンビニ2,300m²は要検討）
