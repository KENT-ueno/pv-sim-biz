# pv-sim-dc 意思決定ログ

設計書 `design_spec.md` が「何を作るか」を書くのに対し、本書は**「なぜそう決めたか」と「判断がどう変わったか」**を記録する。
Claude Code はチャットが変わると会話内容を引き継がないため、**別セッションへの引き継ぎは本書と設計書で行う。**

最終更新: 2026-09-21（第13段階＝風力発電まで）

---

## 現在の結論（2026-09-14 時点）

**pv-sim-dc を独立したHF Spaceとして公開する計画を取りやめ、
既存の `pv-sim-biz` に「データセンターモード」をタブで追加する。**

- 作業対象リポジトリ: `E:\jupyterlab\nedo_solar\pv-sim-biz`
- `E:\jupyterlab\DCCN` で作った Phase 0 の成果のうち、**モデル・タリフ・設計書・検証は移植して活かす**。
  UI層（`run_simulation` / `build_ui` 約800行）と、bizからコピーした計算コア1,255行は破棄する

---

## 判断の変遷

### 第1段階: 新規Spaceとして作る（2026-09-06）

`design_spec.md` §1-1 で決定。理由は3つ。

1. 気温連動PUE・IT負荷・受電容量上限といった**入力の主役がbizと異なる**ため、
   既に複数施設・モード切替・MGで複雑なbizのUIに足すと両方が使いにくくなる
2. 企画の狙いは「一連のMCP成果物を目立たせること」。MCP専用ツールは看板にならず、
   ブラウザで開いて触れる画面が要る
3. （暗黙の前提）**新規Spaceは無料で作成できる**

この判断のもとで Phase 0 を完了させた（commit `aea9165`）。

### 第2段階: PUEを一定値に簡略化（2026-09-14）

ユーザー判断「PUEはデフォルトで1.4、あとは手動設定でよいのではないか」。

- 出典不明の暫定パラメータ9個（α, COP_free, T_free, T_full, COP_ref, T_ref, β, COP_min, PUE_max）を
  UIから排除でき、「根拠の無い数値をデフォルトに埋めない」という本プロジェクト群の作法に最も忠実になる
- 失うもの: §9-1が差別化要素に挙げていた「DCは北に置くべきか」の定量化
  （北はPUEが低いが日射も少ない、というトレードオフ評価）
- **この決定により §1-1 の理由①が大きく弱まった**（後から見れば、ここが再検討の分岐点だった）

### 第3段階: HF仕様変更の判明（2026-09-14）

ユーザーが発見。**無料アカウントではGradio Spaceを新規作成できなくなっていた。**

> Static Spaces are free for everyone. Gradio and Docker Spaces run on compute and
> require a paid plan to create: PRO for personal accounts, Team or Enterprise for organizations.
> （https://huggingface.co/docs/hub/spaces-overview）

**§1-1 の理由③（暗黙の前提）が崩れた。** 詳細は設計書 §1-1・§13。

このとき案A〜Eを比較したが、**5案すべてが「別Space」を保ったままで、
bizへの統合案（＝§1-1の再検討）が構造的に欠けていた。** これは分析側の見落としである。

### 第4段階: biz への統合に方針転換（2026-09-14）

ユーザー提案「産業用＆MGプロジェクトで追加実装するほうが正しいか？」を受けて再検討。
§1-1 を支えていた3つの理由のうち**2つが既に崩れていた**ことが確認され、統合が最善と判断。

さらにユーザー提案「**タブで切り替える**」により、残っていた理由①も解消した。

### 第5段階: UI構成をタブ方式に決定（2026-09-14）

**需要設定セクションのみをタブで分割し、他は共通にする。**
全体をタブにするとPV面設定（8面×5項目＝40コンポーネント）等が二重定義になり app.py が肥大するため。

```
📍 地点選択                          ← 共通
⚡ 需要設定
   ┌[産業用・MG]─[データセンター]┐   ← ここだけタブ
   │ 施設プリセット   IT定格容量  │
   │ 複数施設合算     IT負荷率方式│
   │ モードA/B        PUE         │
   └─────────────────────────────┘
🔌 受電・売電（受電上限はDC時のみ表示） ← 共通
🔲 PV面設定（8面） / 🔋 蓄電池 / 💰 料金・経済性 / ▶️ 計算  ← 共通
```

出力側（月別・日別・デマンド・容量探索の4タブ）も共通。結果テキストのみDC時にPUE等を追加。
モードは `gr.Tab.select()` → `gr.State` で追跡し、`run_simulation` が `demand_source`
（"industrial" / "datacenter"）で需要生成器を切り替える。
**既存の計算パイプラインは (365,48) 配列を受け取るだけなので下流は一切変わらない。**

### 第6段階: 段階2・3の実装（2026-09-19）

上記の方針どおり実装した。実装中に下した判断と、確認できた事実を記録する。

**判断**

- **`run_simulation` のシグネチャは末尾にキーワード引数（`demand_source`, `dc_args`）を足すだけ**にした。
  省略時は産業用で、既存の呼び出し（`mcp_tools` 等）は無変更で動く。DC入力は個別引数でなく辞書
  `dc_args` にまとめ、キーの並びは `DC_INPUT_KEYS`。UIの `on_click` が末尾の引数から `zip` で復元する
- **DC需要の生成・検証は `resolve_dc_demand()` に切り出した**（`run_simulation` 本体の分岐は数行）。
  DCCNにはなかった入力検証を追加（IT容量>0、IT負荷率は0超〜100以下、PUE≧1.0）。既定値は変えていない
- **PUEモードのUIは出さない**（一定値のみ）。第2段階の決定（PUEは一定・ユーザー入力）に従い、
  気温連動を選べるように見えるUIは置かない。`build_pue_30min` の気温連動は `NotImplementedError` のまま
- **北海道電力タリフ表はUIに接続していない**。CLAUDE.mdの指示「料金は共通のまま」に従い、DCも共通の
  「電気料金設定」（既定=東京電力EP）を使う。代わりにDC結果へ「ピーク2,000kW以上なら特別高圧が目安」という
  契約種別の食い違い警告を出す（既存の `contract_power_kw <= 2000` の境界と整合）。
  タリフ表の接続は下記のとおり不要と決定
- 蓄電池の充放電がゼロのときの注記と、`-0.0` の丸めは**DC時のみ**（産業用の出力は1文字も変えない）。
  注記文は「料金が年間単一単価」と断定しない（bizの共通料金は東京電力EPで夏季/他季の単価が異なるため）。
  日内の料金差（時間帯別単価）が無い点は両タリフに共通なので、その言い方にした

**確認できた事実**

- 産業用の既存挙動は不変: 5シナリオ（単一/複数施設・LP・ルールベース・リース/PPA・MG・両面・逆潮流禁止・需要なし）×
  7項目（結果テキスト・デバッグ・グラフ3種・状態）の出力を変更前後で保存→照合し、**35項目すべて同一**
- DC経路は移植元DCCNと一致: 需要生成27パターン・PUE・DC需要・PV年間発電量がすべてビット単位で一致
- 設計書 §5-7-6 の実測値を再現: 札幌・中規模5,000kW・PV5,000kW（方位角180°）で契約電力 6,417.9 → 6,285.3 kW
- **UIの「方位角」欄が未入力でも0を送り、既定の面1が北向きで計算される既存不具合を発見し、同日に修正した**
  （DC統合とは無関係。`gr.Number(value=None)` が0を送る仕様を最小アプリで確認 → `gr.Textbox`（空欄=未指定）に変更。
  詳細は CLAUDE.md「修正済み」）。設計書の実測値との0.8kWの差はこれで説明できた。
  **既定入力の発電量が約+48%になる挙動変更**（従来が過小）。本番（main）には2026-09-19に反映済み
- **北海道電力タリフ表のUI接続は不要**（ユーザー判断 2026-09-19）。料金は共通の「電気料金設定」に手入力で足りるため。
  上の「未決定」は解消。`TARIFFS` 等は段階1で移植済みのまま残す（DC結果の契約種別の目安警告で `resolve_voltage_class` を使用）

### 第7段階: 段階5（受電上限制約）の実装（2026-09-19）

段階の順序は「段階5（受電上限）→ 段階4（MCPツール）」とした。段階5で `optimize_battery()` に引数が増えるため、
先にMCPツールを作るとスキーマ（外部に公開されるインターフェース）を後から作り直すことになる。

**ユーザーからの入力**

- 日本の受電上限は電圧階層の境界が実質的な制約になる: 高圧6.6kV＝2,000kW未満、22・33kV＝10,000kW未満、
  それ以上は66kV。**154kVは考えなくてよい**。→ 上限の選択肢はこの境界そのものにする
- **需要を調整すること（IT負荷のシフト）も上限を守る手段として考慮すべき**（変数が多いので段階を分ける）

**判断**

- **上限は境界の1kW下**（1,999 / 9,999kW）にする。`resolve_voltage_class` は2,000kWちょうどを特別高圧に分類し、
  LPは上限に張り付くのが通常なので、境界値のままだと「高圧に収める」という意図から外れる。
  プリセットは `VOLTAGE_CLASS_THRESHOLDS` から導出し、定義の二重化を避けた
- **上限を守れないとき（実行不可能）は診断メッセージを返す**（ユーザー選択: 診断＋必要量の目安）。
  厳密な最小構成の探索はしない。必要条件3つ（energy/power/capacity）で判定し、必要量は下限の目安と明記する。
  必要条件が破れていればLPを解かず即座に返す（CBCの実行不可能判定を待たない）
- **蓄電池が「なし」「ルールベース」のときは上限を強制しない**（ユーザー選択: 超過判定のみ表示）。
  ユーザーが選んだ蓄電池モードを勝手に変えない。導入後ピークが上限を超えるかの判定と必要量の目安だけを出す
- **上限ありのLPだけ周期SOC**（初期SOC＝年末SOC）。年初に蓄電池が空から始まる人工的な実行不可能を避ける。
  上限なしのLPは従来のまま（産業用の出力を変えない）
- 受電上限の入力は**DCタブの中**に置いた（設計の当初図は共通セクション内の「DC時のみ表示」だった）。
  表示制御が不要になり、産業用タブで選んだ値が残って効いてしまう（隠れた入力値の残留）問題も構造的に避けられる。
  `resolve_dc_demand` が上限を解決するので、産業用モードには上限が存在しない
- **需要側の調整は段階5では入れない**（ユーザー選択: 段階5を先に完成）。当初は「段階6」として次に実装する予定だったが、
  直後に**保留へ変更した（下の第8段階）**

**確認できた事実**

- 診断は健全: 乱数の小問題60ケースで「診断が守れないと言う ⇔ LPが実行不可能」が完全に一致
  （充電電力を十分大きくすると3条件は必要十分。不可能42件/可能18件）
- 産業用の既存挙動は不変: 段階2・3と同じ5シナリオ×7項目の出力保存→照合で全て同一（`optimize_battery` の内部を触ったため確認）
- 数値の検算: 札幌・中規模5,000kW・PV5,000kWで、PV差引後の平均負荷は約5,025kW、蓄電池を無限に大きくしても
  必要な上限の下限は約5,059kW（効率損失分だけ平均より高い＝理屈どおり）。「高圧に収める（1,999kW）」は
  この規模では蓄電池では不可能で、診断がそう返す
- 上限が厳しいとCBCが遅い（充電レートを極端に絞ると1回約37秒。時間制限120秒）。実運用の範囲では6〜14秒
- `infeasible_lp`（診断の必要条件は満たすがLPが解けない＝主に充電レート不足）は、実シナリオでは再現しにくい
  （現実的な設定ではどれも解けた）。合成データで経路を確認した

### 第8段階: 需要シフト（段階6）を保留にし、範囲を第一弾＋DC固有機能に戻す（2026-09-19）

**経緯**: 段階5の相談で「需要を調整することも考慮せねばならない」と出て、「柔軟なIT負荷の割合[%]」を段階6として
計画に入れた。実装前の設計相談で、蓄電池で上限を守るのか需要を動かすのかという2つの手段が競合し、割合・物理上限・
遅延の許容など根拠のある値のない変数が増えることが分かった（「何でもあり」になる）。ユーザーから
「目的を見失ってきた。第一弾はDC需要をセットしてPV・蓄電池の需給シミュができればよく、DCならではの機能はその後」と
指摘があった。

**照合結果（DCCNから受け継いだ設計書 §7・§11）**: 需要側柔軟性は当初から「Phase 5〜の将来」「簡易版のスコープ外」
とされていた。段階6は設計書にない項目を、今日の会話で範囲を決めずに足したものだった。

**判断（ユーザー: 「需要シフトは保留にして開発を進める。ここをいじり始めるとよくない」）**

- **段階6（需要シフト）は保留**。設計書 §7 の位置づけ（DCCN Phase 5・将来・簡易版の範囲外）に戻す
- 次の「DCならでは」はDCCNの順序に従う: MCPツール（段階4）→ §7-1「受電増強 vs PV/蓄電池」の比較
- 再開する場合は、蓄電池を優先し、足りない分だけ需要を動かす形（「必要な柔軟負荷割合」を出力として返す。
  割合を入力に置かない）を先に検討する。**実装の前に目的を確認すること**
- 上限を守る手段は蓄電池のみ。診断メッセージから「需要シフトを今後実装予定」の案内は外した
  （実装しない機能を案内しないため）
- 「DC需要を規模で変えて試す」は既に実装済み（規模プリセット4段階＋用途プリセット＋IT容量直接／ラック数×density。
  時間変動はCEC実測形状／日変動／ノイズ）。数値は暫定値と明記済み

### 第9段階: 段階4（DCのMCPツール）の実装（2026-09-19）

**判断**

- **既存の産業用計算経路を再利用する**。`_run_industrial_simulation` に `demand_override`（需要の差し込み）と
  `grid_cap_kw` を省略可能な引数として足し、DC用の別実装は作らない。産業用は引数を省略した呼び出しのままで、
  HEAD版との突き合わせで出力がビット同一（4シナリオ×simulate/validate。MG・PPA・両面・LP・逆潮流禁止を含む）
- **`estimate_pue` は作らない**。PUEは一定値で地点に依存せず、意味のある出力がない。気温連動PUEを再開するときに追加する
- **受電上限を守れないことは `error` にしない**。ユーザーにとっては「答え」（この条件では守れない、必要量の目安はこれ）であり、
  エージェントが次の提案（上限を上げる／蓄電池を増やす）をするための情報を持つため、`grid_cap_infeasible: true` の
  診断結果として返す。入力の不備だけが `error`
- **MGはDCツールで扱わない**（`mg_*` 引数なし）。UIのDCタブは共通のMG設定が効くが、MCPでは需要施設が1つの
  DCで、束ねメリットの前提（複数施設）が成り立たないため
- **DC入力は簡易キー**（`housing` / `hv_under_2000kw` など）にして、日本語ラベルとの対応は `_dc_maps()` に集約。
  対応が app.py の定義と食い違わないことをテストで確認する
- 契約種別（高圧/特別高圧）の既定は `high_voltage`（UIと同じ）。ピークが2,000kW以上なら警告で知らせる。
  自動で切り替えない（結果に効く既定値を黙って変えないため）

**確認できた事実**

- UI（`run_simulation`）とMCP（`simulate_dc`）で、年間系統購入量・導入後ピークが一致（LPも決定論的に同じ）
- `gr.api` 経由でHTTP（`/gradio_api/call/…`）でも動作。スキーマは7ツール
- 気づき: `simulate_*` ツールは引数の説明が空（産業用の既存の挙動と同じ）。`validate_*` に全引数の説明を置く運用

### 第10段階: 「受電増強 vs PV/蓄電池」の比較機能は作らない（2026-09-19）

**検討**: 設計書 §7-1 の比較（受電増強か、PV・蓄電池で上限内に収めるか）を段階として実装するか検討した。
既存の産業用にこの比較機能はなく、あるのは「高圧→特高で受電設備工事費27,500円/kVAを初期投資に足す」処理だけ。

**既存の単価で試算した結果**（東京電力EPの既定料金、DC・CEC形状・負荷率80%・PUE1.4。参考値）:
- 特高化は電気代が年22〜75M円安く、工事費（27,500円/kVA）を約2.4年で回収する。規模に依らずほぼ一定
- 蓄電池で1,999kWに収められるのは、ピークが境界をわずかに超える範囲（IT 1,600〜1,700kW程度）だけ。平坦な需要は
  平均が境界に近づくと蓄電池では守れない。収められる場合も蓄電池の費用は特高化の数倍になりうる（1,700kWで約2億円 vs 約60M円）

**ユーザーの指摘**:
- 27,500円/kVAは**高圧の受電設備の単価で、特高ではない可能性が高い**。特高の単価はもっと高いはずで、高圧→22kVでも
  特高が有利になりやすいという今の試算は前提が崩れている（出典の記録もない）
- 22・33kV→66kV（10,000kW境界）の単価は存在しない
- 蓄電池でできることは単価が高く、たかが知れている（試算と一致）
- 系統増強待ちや高圧・特高の違いは単価で表せず、現時点の単価での比較は意味をなさない

**判断（ユーザー: 「比較機能は不要かもしれない。とりあえずシミュレーションできます、くらいに収める」）**
- 比較機能は作らない。「参考比較の1行」を診断に足す案も、単価が信用できないため取り下げた
- 「シミュレーションできる」の範囲は、受電上限の診断（段階5: 守れるか／守れないなら必要な蓄電池の下限の目安）までとする
- 27,500円/kVAは値を変えず、出典が確認できていない旨を CLAUDE.md・設計書に記録した（手入力で変更可）。
  特高の単価が分かれば、産業用の「高圧→特高」の初期投資も改善できる
- 産業用（+MG）への展開は不要になった（比較自体を作らないため）

### 第11段階: MCP実機検証の準備と文書整理（2026-09-20）

**MCPプロトコル層の検証で見つけた不具合**: 汎用MCPクライアント（`initialize` → `tools/list` → `tools/call`）で本番に接続し、
ローカルの直接呼び出しと突き合わせたところ、`capacity_factor_pct` が `numpy.float64` のまま返り、MCP経由だと
**数値でなく文字列（`"11.5"`）** になっていた。ローカルの直接呼び出しとHTTPの `/call` では数値として比較が通るため、
これまでの検証（産業用のCodex検証を含む）では検出できなかった。産業用ツールと `estimate_pv_generation` にも以前からあった。
まず個別に `float()` へ変換して修正し（`f86f617`）、その後、公開ツール7つの成功系の `return` を `_jsonable()` で包む方式に一本化した
（機構を1つに絞ると差分が小さく、grepで確認でき、今後の変更でも再発しない。デコレータ方式はGradioがシグネチャ・docstringから
MCPスキーマを作るため避けた。公開7ツールのシグネチャ・docstringは変更前と同一）。テストは「JSON標準の型だけ」の検査に拡張した。**pv-sim-gh・pv-sim-fip も本番で同じ症状**
（`estimate_pv_generation`。gh は `payback_years` も）を確認し、**両リポジトリでも `_jsonable()` に統一して修正済み・本番確認済み**
（gh `3abba28`／fip `f2a50f9`。修正前後の出力は gh 23条件・fip 33条件で完全一致）。

**判断**
- 実エージェント（Codex / Claude Code）での検証は、こちらから実行できないため**手順書**（`docs/dc_agent_verification.md`）にした。
  プロンプト・期待値（ローカル計算値）・判定基準・結果記録欄を用意。プロトコル層の検証結果は同書に記録
- 文書整理: 設計書 §13 の古い項目（公開手段の案A〜E。統合で解決済み）は **付録A** に移して圧縮。§11 に
  DCCNのPhase番号と biz統合後の段階の対応表を追加。`pv-sim-fip/docs` の3文書（agent_design・mcp_guide・example_prompts）に
  DCツールと検証の状況を反映（コミットは fip 側でユーザーが行う）

**Codex での実機検証（同日実施）**: 4シナリオ＋産業用（福岡・病院・LP・リース）で、全項目がローカルの計算値・過去の参照値と一致。
2段階プロトコルと警告・caveatsの伝達もdocstringだけで期待どおり動いた。**受電上限を守れない結果を `error` にせず診断として返す設計
（第9段階）**が、エージェントの説明（理由と次の一手）の質につながることを確認。Codex の指摘は「特定日の30分カーブを取得する
ツールが無い」（検討候補。CLAUDE.md）。S4で、エラーがあるのに規模プリセットの暫定値警告も出る軽微な冗長も見つかった（未対応）。
**Claude Code での実機検証（同日実施）**: リポジトリ外の空フォルダの新規セッション（`pv-sim-biz` のMCPだけを登録）で、
前置きなしのプロンプトを貼付。S1〜S4の全項目が一致。2段階プロトコルはツールの説明文だけで守られ、用途・地点の推定、
診断の読み解きも適切だった。**設計書 §11 の Phase 4 完了条件（Claude Code / Codex 双方から実測一致）を満たした**。
検証のため、ローカルの Claude Code のユーザー設定に `pv-sim-biz` のMCPを登録した（不要なら `claude mcp remove pv-sim-biz --scope user`）

### 第12段階: 後片付け（2026-09-20）と、本番での実測

- **S4の冗長な警告を修正**: 入力にエラーがあるとき、「規模プリセットの区分は暫定値」の警告を出さない（直す点だけを伝える）。
  エラーがなければ従来どおり出る（Codexの指摘）
- **`__pycache__` を管理から外した**（biz・gh。fipは元から対象外）。`.gitignore` に `__pycache__/` と `*.pyc` を追加。
  gh は fip と同じ方針の `.gitignore`（CLAUDE.md・テスト・`__pycache__` を無視）を新設。個人の作業ファイル2件は
  `.git/info/exclude`（ローカルのみ）で無視。テストの実行で書き換わる結果ファイル（実行時間だけが変わる）は元に戻した
- **本番の遅いLPの実測**（受電上限が厳しい条件。札幌・IT1,000kW・上限1,190kW・蓄電池10kWh）: 最大充電5 kW→9.5秒、0.3 kW→11.9秒、
  **0.03 kW→41.1秒**（ローカル実測の約37秒とほぼ同じ）。HFの中継で切れず、CBCの制限（120秒）にも余裕がある。
  これより厳しい条件（極端に小さい充電電力）は現実的ではないため、対応は不要と判断
- **本番UIの目視確認**: データセンタータブと「系統受電上限」があり、既定の計算（中規模5,000kW）の結果が期待値どおり
  （施設49,056,000 kWh/年、ピーク6,417.9 kW、PV 5,038.5 kWh＝方位角の既定が南向き）。コンソールのエラーは404が1件のみ（原因は未調査。動作に影響なし）

### 第13段階: 風力発電（オフサイトPPA）を発電源に追加する（2026-09-20。設計は `wind_design_spec.md`）

**決定:** 太陽光に加えて風力を第2の発電源として、任意に「片方のみ／併用」できるようにする。
問いは「24/7に近づけるには何をどれだけ積むか」。エリアは**北海道・東北のみ**、需要地も道内・東北に限る。
形状はエリア需給実績（暦年2025・30分値）、水準（設備利用率29.1%・PPA単価11.96円/kWh）は調達価格等算定委員会の資料。

**次の案を検討して行き止まりだった（同じ検討を繰り返さないための記録）:**

- **A（DCの排出係数・エリア別CO2）**: 日本では各電力の係数の差が小さく実務上意味が薄い（ユーザー指摘）
- **B（受電増強 vs PV/蓄電池）**: 実質的な上限は予算で、受電設備の単価が曖昧なので実装しても意味が薄い（第10段階と同じ）
- **D（出力抑制リスク）**: 本ツールはオンサイトなので出力抑制リスクは存在しない
- **JEPX連動の産業用メニュー**: 産業用に市場連動はないと断言できる（ユーザーは市場連動最多の新電力の元社員）
- **METPV-20 の風速（要素7）を風力に使う**: 使えないと実測で確認した。日射との日内相関が11地点平均で+0.78
  （太陽光と同時に吹く結果になる）、ハブ高への外挿指数 α を 0.10〜0.30 で振ると設備利用率が7.2〜21.9%と3倍動き、
  実際の陸上CF（20〜25%）がその帯の内側に入る。**仮定が答えを作ってしまう**。エリア需給実績（東北: 夜/昼 1.01、
  太陽との相関 -0.38）が決定的な反証になった
- **windpowerlib 等のOSSエンジン**: 風速→パワーカーブの経路を採らないので不要
- **renewables.ninja**: CC BY-**NC**（非商用）で営業ツールに使えない
- **REPOS（環境省 風況マップ）**: 年平均風速のみで時系列がなく、風速→CFの変換に仮定が要る。将来の地点別補正の候補

**なぜ国は風力の時系列を出していないのか:** 日射は空間的に滑らか（50地点＋換算で数%の精度）なので公的データから設計できるが、
風は地形に敏感（1kmで2倍）で、日本の事業者は**1年間の現地実測**（マスト＋ライダー）＋長期補正で作る。国は
スクリーニング用の地図（NEDO・環境省）までしか整備していない。

**ユーザーの指摘で直ったこと（重要）:**

- 私は「エリアの発電所容量は要らない」と言いかけたが、実際は既定の24.6%を「最大÷0.85で仮定した容量」から導いていた。
  **エリア需給実績は容量も設備利用率も書いておらず、水準は仮定（入力）が要る。** 容量と設備利用率は同じ情報で、どちらか一方が要る
  （東北: 4,886GWh ÷ (8.76×0.246) ＝ 2,267MW ≒ 推定容量2,264MW）。そこで水準を公的な出典（調達価格等算定委員会）の
  想定値に置き換え、形状だけをエリア需給実績から取る（DCの需要モデルと同じ「水準と形状の分離」）
- **設備利用率は「仮定」でなく「入力」**（PUE・IT負荷率と同じ扱い）。オフサイトPPAの買い手は事業者の提案書から実際にこの数字を得られる
- 設備利用率の既定は新設の想定値 **29.1%**（既設の平均 東北24.6%・九州20.1%は古い小型機を含むので、新規PPAには使わない）

**確定した前提（ユーザー承認）:** 風力は受電点に届く／pay-as-produced／FITは風力の余剰に適用できない／
**太陽光（平年値）と風力（2025年実績）の同時性は担保されない**（意味があるのは月別・時間帯別の平均的な補完関係まで。最大の限界）

**実装で判明したこと:** 北海道のCSVは月の日数が少ない月を空行で1488行に埋めている（詰め物として捨てる）。
NIIGATA は東北電力NWの供給区域（東北6県＋新潟）なので東北エリア（設計書の当初の7地点を8地点に修正）。
出典URLは6エリアで確認したが、対象は北海道・東北の2エリアに限った。

**W2c: 託送料金の反映漏れ（ユーザー指摘 2026-09-20）と作り直し:**
「発電単価kWhだけでなく、オフサイトPPAなので託送料金を入れないといけないが、反映漏れはないか」への答えは**あった**。
しかも私が前提1で「託送料はPPA単価に含める」と承認を得ていたが、それは**節約額の側を正さない**ので不十分だった。
W2 は風力を敷地内の太陽光と同じ扱いで購入量から引いていたため、風力に次を付けていた（実測、仙台・需要の100%相当・PPA11.96円）:
基本料金の削減（契約電力が下がる）・再エネ賦課金の削減・託送の従量分と小売手数料が無い。風力のみの純メリットは
＋65.8万円/年→（基本料金と賦課金を外すだけで）−9.8万円/年に反転し、託送の従量分と手数料を引くと−97.6万円/年になった。
→ **受電点の基準で料金を作り直した**（`offsite_receiving` / `offsite_cost_after`。設計書 §5-4）。実装後の実出力は
−975,605円/年で、事前の手計算（−97.56万円）と一致した。

- **蓄電池の運転（収支）は太陽光＋風力を合わせた発電のまま**、料金だけ受電点基準に組み替えた。風力の余剰を蓄電池に貯めて
  凪の時間に使う効果（24/7の主役）を残すため。代償として、受電上限（DC）と最適容量探索は併用不可（明示エラー）にした。
  これは W2d（LPに風力の配達量の変数を足す）で解消した（下の「W2d」）
- **二次情報は必ず一次資料で確認する。** 検索の要約が「東北 高圧の託送 電力量料金 4.40円/kWh」と返したが、
  一次資料（東北電力NWの届出PDF）は **2.15円/kWh**（税込）だった。確認せず使うとXを2倍以上に誤っていた。
  METIのPDFは403で取得できないことがあり（User-Agent付きでも）、ユーザーがダウンロードしたPDFを読んだ
- **小売手数料Yに公的な出典はない。** 自然エネルギー財団の推定（高圧3円・特高1.5円、2023年度全国平均）を既定にして「推定値」と明記
- **オフサイト太陽光（ユーザーの問い）**: 電源の種類と届き方は別の軸。W2c の経済性はオフサイト電源のリストを受け取る形にした。
  オフサイト太陽光は W6 候補（Xを用意できたエリア内）。今は実装しない
- **私の見落とし:** 前提1を提案した時点で、フィジカルPPAの費用構造（託送・賦課金・手数料が削減されない）を
  一次資料で確認していなかった。ユーザーの指摘がなければ、風力の得を過大に見せる数字を出し続けていた

**W2b: MG（モードB）への反映（2026-09-20）:** 風力はMG事業者から見て「網内に供給する電源が1つ増える」だけ、と最初は考えたが、W2cで分かったとおり届き方が違う。MG事業者の収益＝網内に供給した全量×網内単価＋束ねメリット、費用＝運営コスト＋風力の調達費用
（PPA支払＋届いた分の託送・賦課金・手数料）。契約電力は風力では下がらないので束ねメリットは増えない。
PPA（MG）の単価は（投資の回収＋風力の調達費用）÷ 網内に供給した全量で逆算し、P-IRR≒目標IRR＋束ねメリットの性質を保った。
既定のMG（自営線6,000万円）に需要の100%相当の風力を足すと、風力の調達費用が売電収入を上回りP-IRRは算出不可になる（実際にそうなる。風力を買うほどMGが赤字になる条件は、PPA単価で回収する形でしか成立しない）。
- **PPA単価の分母を実測に置き換えた:** W2 は自家消費量を発電量比で太陽光分に按分していたが、風力は使い切れず無駄になる分が多く、按分は不正確だった。**自家消費 ＝ 敷地内の太陽光・蓄電池分＋風力の配達量**（蓄電池なしで厳密）なので、自家消費 − 配達量を使う
- **既存のバグを1件直した:** `_calc_irr` は全期間のキャッシュフローが負だとニュートン法が発散し、`(1+r)**t` のオーバーフロー（`OverflowError: Result too large`）が画面のエラーになっていた。風力の調達費用が大きいMG（複数施設）で現実に起きた。符号が変わらないIRRは存在しないので None（算出不可）を返し、オーバーフローは二分法へ回す。正のキャッシュフローの結果は変えていない

**W3: UI（2026-09-21）:** 風力の入力は `on_click` の末尾に追加して既存の並びに触れない。任意入力は空欄＝既定値のテキスト欄（`gr.Number` が空欄を表せない既知の仕様のため。方位角欄と同じ）。調達エリアは入力でなく表示。UI配線は、ボタンの関数をUIの入力コンポーネントの並びどおりに呼ぶテストで守る（位置ずれが最大の危険）。UIの既定は施設なし（需要なし）なので、初めて風力を試す人が最初に踏む「需要未設定で風力ON」は、需要の設定を促すエラーにした（テストで例外が出て発見）。

**W4: MCPツール（2026-09-21）:** 風力は `wind`（辞書）と `pv_enabled` を、MCPツールの**末尾の任意引数**として足した（省略時は従来とバイト同一）。MCPの計算は `run_simulation` を呼ばず独立に持っているので、風力の経済性はUIと同じ関数を呼んで移し、二重実装を避けた。既定値は検証時に解決して `normalized_params` に出す（エージェントがユーザーに確認できるように）。面のPpeak=0はMCPでは入力エラー（風力のみは `pv_enabled=false` と明示。UIのように黙って風力のみにしない）。テストが甘かった点: 風力のみの回収年数を『なし』にする分岐は、テストの風力のみが赤字だったため元から『なし』になって効いていなかった（変異テストで発見）。メリットが正になる小規模ケースを足した。
**Gradio のMCPは結果を Python リテラル表記の文字列で返す**ことが、プロトコル層の確認で分かった（数値は数値のまま。numpy型だと `'11.5'` のように引用符が付く、という既知の罠の現れ方）。

**W5: 文書の仕上げ（2026-09-21）:** README（機能・MCPツール・構成・出典）、architecture.md（§14 風力）、設計書（DC設計書から風力への参照）、実機検証の手順書 `wind_agent_verification.md`（期待値は `mcp_tools` を直接呼んで確定）を整えた。README は公開向けの説明なので、風力の限界（北海道・東北のみ、同時性は反映されない、系統受電上限・最適容量探索とは併用不可＝W2dで解消）を明記した。**実機検証（Codex・Claude Code）は本番に反映してから行う**（push はユーザーの指示待ち）。

**W2d: 蓄電池LPを受電点の基準で解く（2026-09-21）:** 受電上限（DC）・最適容量探索と風力の併用を、LPそのものを受電点の基準にして解いた。
`optimize_battery` / `optimize_battery_capacity` に `offsite` 引数（省略で従来とビット同一）。変数は受電量 R と風力の配達分 w（w ≤ R、w ≤ 風力発電量）で、
売電・出力抑制はそのコマの太陽光の余剰以下、目的関数は 基本料金×max(R) ＋ (R−w)×小売単価 ＋ w×(託送＋賦課金＋手数料) − 売電。風力PPA支払は定数なのでLPに入れない。
受電上限は R の上限。風力は上限を守る助けにならないので、事前診断は太陽光だけで行う。設計は設計書 §5-4。
- **実装中に見つけた誤り: 受電点の相殺。** 受電点では買電と売電が相殺されるので、同じコマで受電と売電が同時に正にはなれない。LPの変数では正にできてしまい、
  売電単価（FIT 19円）が風力の配達単価（≒9.3円）より高いと「風力を受電して太陽光を売る」運転をLPが選んだ（メリット 312万→253万円/年の差。最適容量探索では
  容量ほぼ0でもこの分が乗り、蓄電池のP-IRRを過大にする）。動作の確認で、受電量と売電が同じコマで両方正になっているのを見て気づいた。
  **対処**: LP内の売電単価だけを `min(売電, 風力の配達単価, 小売の最小単価) − 0.01円` に抑え（受電量と売電量の関数を凸にして、LPが相殺した運転を選ぶようにする）、
  実際の売電収入・電気代は返ってきた運転を実際の単価で評価し直す。等号だと同値のコマで同時に正になりうるので、0.01円だけ下げて厳密に損にした。運転の最適性は少し損なう
  （FITと風力を併用する条件でだけ効く）。厳密に解くには買い・売りの排他を二値変数で表す必要があり、17,520コマでは現実的でないと判断した
- **検証**: 独立実装（scipy.linprog。受電点を正味の買い・売りの流れで表す別の定式化）と最適値が一致（売電あり・逆潮流禁止・受電上限［周期SOC］・容量が変数）。
  不変条件（収支・受電量と売電が同時に正にならない・売電は太陽光の余剰だけ・受電量≦上限・ピーク＝受電量の最大）、FIT 19円で裁定が生じないこと、
  LPがルールベース・蓄電池なしを上回ること。風力OFFは70項目・MCP12ケースがバイト一致のまま。実行時間は許容内（最適容量探索で約80秒、風力なしで約65秒）
- **ルールベースは太陽光＋風力の合計で運転する**ままにした（実測: 契約電力が68.4→81.1kWに上がる。風力を蓄電池に貯めて受電量のピークを上げるため。LPなら39.5kW）。
  これはルールベースの限界として設計書に書いた（→ W2e で解消）
- MCPは `validate_dc_params` の「風力×受電上限はエラー」を、警告（受電上限は風力の配達分も含めた受電量に対する上限）に変えた

**W2d のUI確認（2026-09-21・Firefox）:** 風力×受電上限×LP（DC）はUIの数値が本番MCP・ローカル計算と完全一致（導入後ピーク1,071.3kW・年間経済メリット65,467,001円・時間一致率75.0%。LPの最適化コスト＝導入後の電気代−売電収入も1円の丸めで一致）。
ルールベースのまま風力を足すと契約電力が上がる（1,283.6→1,739.9kW）ことも見た（風力を貯めて受電量のピークを上げる。設計書 §5-4 の限界。W2e候補: ルールベースは太陽光の余剰だけ貯める）。
風力×最適容量探索（仙台・役所）は最適容量64.9kWh・年間コスト削減2,257,949円/年で期待値と一致（最大充放電を100kWにしても1,500kWでも同じ。容量が小さく充放電が効かない）。
**UIで見つかった既存の不整合（W2dとは無関係。初回デプロイからの挙動）:** 最適容量探索モードでは上部の需給・料金は蓄電池なしで計算するのに、上部の設備投資は入力欄の容量（別モードで入れた3,000kWhが残っていた）で蓄電池の費用を数えていた。回収年数が104.3年と出て誤解を招く（段階1の12.2年が正しい）。
→ 最適容量探索モードでは上部の設備投資に蓄電池を入れず、その旨を表示するよう修正した（`test_wind_lp.py` PASS 101。風力なしのスナップショットは最適容量探索のシナリオの表示だけが変わる）。

**W2e: ルールベースの蓄電池を風力に対応させる（2026-09-21）:** W2dのUI確認で、ルールベース（既定の蓄電池モード）に風力を足すと契約電力が上がる
（1,283.6→1,739.9kW）のを見て、直した。`simulate_battery` に `offsite_gen` 引数（省略で従来とビット同一）。蓄電池は太陽光の余剰だけを貯め、不足分に **風力→蓄電池→小売** の順で充てる。
- **1回目の設計が悪かった**: 「蓄電池が先、風力が残りを埋める」にしたら、蓄電池を足すと年間メリットが**下がった**（5,631万円。蓄電池なし5,852万円）。
  風力（約9円/kWh）を蓄電池で置き換えるために、貯めずに売れた太陽光の余剰（FIT 19円）を捨てていたため。順番を「風力が先、蓄電池は風力で賄えない残り」に直して
  蓄電池なし5,852万円 < ルールベース5,917万円 < LP6,547万円になった。数値を見て設計を直した（先に設計だけで決めていたら気づかなかった）
- **実装の誤りも1つ**: 受電量 `offsite_receive` に放電**前**の不足を入れていた。実際に送配電網から受けるのは 風力の配達分＋小売購入（放電後）で、放電で減る。
  契約電力への蓄電池の効果が消える誤りで、コードを見直して気づいた（テストでは参照実装との照合で検出できる形にした）
- **検証**: 参照実装（別コード）と充放電・配達・購入・受電量・売電・抑制が一致、収支・不変条件（風力では充電しない・受電量は蓄電池なし以下・放電は風力で賄えない残りだけ）、
  従来の合算運転が受電量を押し上げる例（10→40kWh/コマ）、UI・MCPとの数値一致。変異11件を検出（最初の1件はMCPの照合不足で、メリット・配達量の照合を足して検出）。
  風力OFFの出力は変わらない（UI 70項目のうち最適容量探索の表示だけが前回の意図した修正で変わっている。MCP 12ケースは一致）
- **限界**: 風力を貯めて凪の時間に使う効果はルールベースでは出ない（LPで見る）。ルールベースの契約電力は下がりにくい

**MCP: 未知のキーを明示エラーに（2026-09-21）:** Codex（Work）の検証で、エージェントが `faces` に `capacity_kw`（正しくは `ppeak_kw`）を送り、`facilities` や列挙値も誤って往復が増えた。
列挙値の誤りは選択肢が出るが、`faces` の未知のキーは**黙って無視され、既定値（方位角180°・傾斜30°）で計算されうる**（`azimuth` の綴り違いなどで、誤った数字が返る）。
`faces`・`facilities` の未知のキーを、よくある別名表と綴りの近さ（difflib）で正しいキーを示す明示エラーにした（`_unknown_key_errors`。`wind` は元から未知のキーを拒否）。
正しいキーだけの入力は従来どおり（MCP 12ケースは一致）。`test_mcp_tools.py` PASS 53。

**W6 の設計（2026-09-21・ユーザー承認）:** オフサイト太陽光（需要地とは別の場所の太陽光発電所から、同じ電力エリア内でオフサイトPPAで受電する）。設計は `wind_design_spec.md` §8。実装は未着手（Sonnet）。
- **ユーザー判断**: エリアは **B案**（全50地点を発電地点に選べる。託送Xの既定値は北海道・東北だけで、他エリアは手入力必須＝一次資料のない値を既定に埋めない）。
  太陽光と風力を**両方オフサイトで持てる**。24/7 は電源ごとに行を分ける。オフサイト太陽光の限界（時間一致率が伸びにくい）を結果に出す。電源ごとにX・Yの入力欄を持つ。
  「本来は複数DC（会社単位）でそれぞれの自家消費＋オフサイトで調整するが、今回は自家消費が主（オフサイト主で制御する仕組みは無い）。オフサイトで不要になった電気はインバランスになり損失が大きい」
  → **現行の保守的な扱い（無駄になった分は対価ゼロの全額損失）を変えない**。複数DCは **W7（今後の課題）**
- **PPA単価は積み上げ式（ユーザー指摘で改訂・案1）**: 当初は入札の平均落札価格（太陽光 8.17円・風力 11.96円）をそのまま既定値にしようとしたが、**乱暴だった**。
  入札価格は発電事業者の必要収入を積み上げた政策上の価格で、オフサイトPPAの相対契約の単価ではない。オフサイトの費用は託送・需給調整・小売／アグリゲーション費用などの積み上げで、
  その単価の構造は公開資料になく、推測しかできない。→ **推測が必要な部分は調整できる入力にし、各値の出典の有無（A〜D・U）を md・結果・MCP に明記する**（プログラムとしての誠実さ）。
  発電側は、資料（令和7年2月3日の意見 p.118・p.121-122）の想定値（資本費・運転維持費・設備利用率・IRR・年数）から資本回収係数で逆算する（オンサイトPPAの逆算と同じ考え方）。
  実測: 太陽光 8.53円（IRR4%・25年）〜14.23円（10%・15年）、風力 12.60円〜17.31円。**想定で大きく動く**ので、感度を結果に出す。
  発電側課金相当・出力抑制・営業費・インバランスは出典のある値がなく、**0（未算入）と明示**。需要家側の需給調整・小売／アグリゲーションは推定値Y（一括）。
  「入札価格を参考にした」ことと「資料の想定で積み上げた」ことは、ほぼ同じ情報（入札価格が同じ想定から作られているため）。違いは前提が見えて調整できること。
  風力の既定は W6 では従来の 11.96円のまま（既存の出力・本番で確認済みの期待値を変えないため）。積み上げ式（12.60円）への切り替えは W6 の後にユーザーが判断する。
  オンサイトPPA（モードA）は投資額から逆算する式なので、この問題はない（`kW単価×容量`から kWh 単価を出す）。
  第102回の資料（`102_b01_00.pdf`）は 403 で取得できず未確認。
  風力の11.96円（第112回＝2025年度入札）より約1年古い。第112回の「太陽光発電について」があれば差し替える。同資料は「FIT/FIPによらずPPAで導入するモデルが確立しつつある」とも書いており、相対契約の実勢とは乖離しうる
- **負担を下げる絞り込み（承認済み）**: 容量はkW指定のみ／両面パネルなし／K値は敷地内と共通／エリア違いはドロップダウンで絞らず計算時にエラー／面は1面。
  太陽光は発電量を日射から計算できるので、**風力のような形状データが要らない**（エリアに依存するのは託送Xだけ）
- **負担が小さいと分かった根拠（実測）**: W2c〜W2e で下流は「オフサイト電源のリスト」を受け取る形になっており、**2電源でも変更不要**。
  ①配達量の按分は経済性に影響しない（2電源の順序を入れ替えても総額が完全に一致。電源ごとにYを変えても同じ）②2電源のLPは、合算した1電源のLPと最適値が完全に一致（差0.00）③電源ごとの配達量・無駄になった量がそのまま取れる。
  作業は「単一の `wind_info` を決め打ちしている約10か所をリストに一般化する」ことが中心で、LPの定式化の作り直しは無い
- **地点→エリア**: 50地点のうち49地点を10エリアに対応させた。**敦賀は供給区域（北陸／関西）を一次資料で確認できるまで未対応**。長野・静岡・浜松・津・岐阜・山口は W6-0 で一次資料により確認する
- **限界（結果・MCP・READMEに書く）**: オフサイト太陽光は敷地内太陽光と発電の形が似るので、量は増えても時間一致率は伸びにくく、無駄になる分が大きい（結果に「足した効果」を出す）。
  電源ごとにX・Yが違う値を手入力すると、LPの目的関数（発電量で重み付けした平均単価）と事後の評価がわずかにずれる

**W2f: オフサイト電源の費用構造の作り直し（2026-09-22・ユーザー承認）:** 設計は `wind_design_spec.md` §9。実装は未着手（Sonnet）。W6 より先に行う。
- **きっかけ**: ユーザーが、オフサイトPPAの費用を「公的データで決まる（発電側課金・損失率・託送・賦課金）」「公的データからの代理値（PPA発電単価）」「契約依存（バランシング・小売費）」に整理した。
  W2c の構造には、公的データで決まるのに入っていない項目（送電ロス・発電側課金）があり、小売手数料Y（財団の推定）は一括の代用値だった
- **一次資料で確認**: 北海道・東北の託送供給等約款（2026年4月1日実施）で、託送（基本・電力量）・損失率・発電側課金・損失の式の全数値が一致した。
  ChatGPT が示した JPEA の値（1.1円・4.1円）は、最初の検索では別年度・別報告の値（1.0円・3.4円／日経記事の2026年報告の値）しか出ず、**ユーザー提供の原資料で R4年度・高圧の値と定義を確認してから採用した**
- **ユーザーの式を2点直した**: ①PPA使用量は需要ではなく**受電量**（敷地内太陽光・蓄電池の後）との min を**30分ごと**に取る（需要で取ると太陽光と二重の供給、年間で取ると冬と夏が相殺）
  ②再エネ賦課金の対象は総需要ではなく**受電量**（自家消費は対象外）。託送の基本料金は正しい項目だが、小売の基本料金（東京電力EP）に含まれているので別に足さない（足すと二重計上）
- **送電ロスは量で扱う**（到達可能量 = 発電量 × (1 − 損失率)）。費用項目として足すと二重計上。JPEA R4 の送電ロス 0.5円 ≒ 売電価格 11.3 × 4.7% ÷ (1 − 4.7%) = 0.56円で、同じ構造と確認
- **発電側課金は、委員会の単価（入札価格・積み上げ）には含まれない**（委員会の意見 p.117）。実契約の単価は含むことが多い（JPEA の売電価格 11.3円は 1.0円を含む）→ 「自動（既定値なら加算・入力値なら含む）／加算／含む」の選択。割引は変電所次第で 0（未算入＝最大）
- **余剰精算は入れない**（ユーザーの判断: 余剰の収入はPPA事業者・小売のもので需要家のものではない）。代わりに JPEA p.29 の契約形態（完全ミラー＝全量払い 47%／部分ミラー＝使用量払い 53%）から **支払の対象**を選べるようにした。既定は全量払い（保守的）
- **使用量払いの蓄電池LP**: 発電側単価を目的関数に入れると、PPAの電気が小売より高いときに LP が使わない解を選ぶ（契約上ありえない）ので、must-take（来たら使う）として事後に評価する
- **影響（仙台・風力100%・高圧・蓄電池なし）**: 年間経済メリットは 1,952,675円 → 全量払い 1,285,751円／使用量払い 3,459,467円。時間一致率 79.8% → 79.1%。**本番・Codex で確認済みの風力の数値はすべて変わる**
- **W6（§8）への影響**: 費用の構造は §9 を使う。太陽光の PPA発電単価の既定は、委員会の積み上げ 8.53円が JPEA の実案件（売電単価 補助なし 15.0円・補助あり 11.3円、いずれも発電側課金を含む）より大幅に低いと分かり、**要判断**に戻した（推奨は JPEA の 15.0円）

**残り:** W2f（実装。§9-9 の W2f-1〜W2f-5）、W6（実装。§8-11 の W6-0〜W6-6。W2f の後）

**進捗:** W0（データ）・W1（計算層）・W2（`run_simulation` 統合）・W2c（経済性の作り直し）・W2b（MG）・W2d（LPを受電点基準に）・W3（UI）・W4（MCP）・W5（文書）が完了。

---

## 却下した案と理由

| 案 | 却下理由 |
|---|---|
| A. HF PRO課金（$9/月） | **コスト。** 円安下で年17,000円は重いとのユーザー判断（2026-09-14）。加えてPROが同時稼働8 units枠を広げるかは公式ドキュメントに記載がなく、5本目が動く保証がない |
| B. `pv-sim-gen` をリネームして転用 | `gen` を失う。リネームが作成制限を回避できるか未検証。**プロジェクト分離とコード重複1,255行はどちらも解決しない** |
| C. Static Space化（Gradio-lite） | **MCPサーバを公開できない**（静的ホスティングにサーバプロセスが無い）。技術的には可能なことを実測確認済み（設計書§13「案Cの実測検証」）だが、企画目的を失う |
| D. HF以外でホスト | ロリポップ！デプロイナウはPython非対応（Next.js/Nuxt/静的のみ）。マネージドクラウドは月1,348円でPROとほぼ同額 |
| E. 静的UI＋MCPを既存Spaceに相乗り | 実装が2箇所に分裂し、**Claude Code上も別プロジェクトになる**（セッション・メモリ・設定が分離）。scipy版とPuLP版で数値が食い違うリスクを単一実装で抑える必要があり保守が重い |

---

## この決定が解決しないこと（正直な記録）

**本決定は pv-sim-dc の公開先問題を解決するが、ロードマップの残り3本は解決しない。**

将来構想（非公開メモ）では、本プロジェクトの後にさらに3本の開発を予定している。同じ理屈で3本ともbizに積むのは単一 app.py として無理があり、
**3本目・4本目の時点で同じ壁（新規Space作成の有料化・同時稼働8 units枠）に再び当たる。**
今回の判断は解決ではなく先送りである。目の前の1本に対しては最善だが、その認識を持って進めること。

---

## Phase 0 成果の引き継ぎ

`E:\jupyterlab\DCCN`（commit `1d2cf9f` 時点）から biz へ移すもの。

| 成果物 | 扱い |
|---|---|
| `TARIFFS` 3階層テーブル（北海道電力6メニュー）＋ `resolve_voltage_class()` / `get_tariff()` / `get_menu_choices()` | ✅ 移植。既存の東京電力EP定数（`ELECTRICITY_RATE_HV/EHV`）は残し、電力会社の選択肢として追加する |
| DC需要生成器3関数 `build_it_load_30min` / `build_pue_30min` / `build_dc_demand_30min` | ✅ 移植 |
| `docs/design_spec.md`（625行）・`docs/hokkaido_power_voltage_tariff.md`・本書 | ✅ bizの `docs/` へ移す。§1-1・§4-2・§11・§13 は統合前提に改訂が必要 |
| `test_dc_smoke.py` | ✅ 大部分を流用 |
| `_force_utf8_stdio()` | ⏭ 不要。bizには別タスクで適用済み（biz commit `3b9ef7c`） |
| bizからコピーした計算コア1,255行 | ❌ 破棄（bizの自前コード） |
| `run_simulation` / `build_ui` 約800行 | ❌ 破棄。biz統合用に書き直し |

### biz への追加実装スコープ

1. 北海道電力タリフ表＋受電電圧の自動判定（東京電力EPは残す）
2. DC需要生成器（需要設定タブに「データセンター」を追加）
3. `optimize_battery()` に `grid_import_cap_kw=None` を追加（LP制約1本）
   — **MGモードにも有用**なのでDC専用機能ではない
4. UI: 需要設定を `gr.Tabs()` で分割（上図）
5. MCPツール `estimate_dc_demand` / `validate_dc_params` / `simulate_dc` を
   `mcp_tools.py` に追加＋`app.py` に `gr.api()` 3行

**触らないもの**: 既存の計算コア、ComStock需要プリセット、複数施設合算、
モードA（リース/PPA）、モードB（マイクログリッド）、東京電力EPのタリフ。
DCCNで削除した §4-2 の機能群は、bizでは**削除しない**。DCは選択肢の追加である。

---

## 作業上の注意

- **biz は公開中の本番Space。** リモートは2つあり、`origin` = HF Space（**push = 本番デプロイ**）、
  `github` = GitHub。作業はブランチを切って行い、push はユーザーの明示的な指示を待つこと
- Claude Code はチャット間で会話を引き継がない。引き継がれるのは
  **リポジトリ内のファイル**と**プロジェクトのメモリ**（`~/.claude/projects/<slug>/memory/`）のみ。
  判断の経緯は必ず本書か設計書に書くこと
- 実測で判明した重要事実（radiation.dbは50地点／需要が平坦だと蓄電池の価値がゼロ／
  PV単体では基本料金を下げられない）は設計書 §11 に記録済み
