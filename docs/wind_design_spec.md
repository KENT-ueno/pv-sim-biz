# 風力発電（オフサイトPPA）機能 設計計画

**状態:** 承認済み（2026-09-20）。**W0・W1・W2・W2c・W2b・W3・W4 完了**（W4は未コミット）、W5 以降は未着手。
W2c＝ユーザー指摘（託送料金の反映漏れ）による経済性の作り直し（§5-4）
**想定分担:** 本書＝設計（Opus）／実装＝別セッション（Sonnet）
**前提となる調査:** `docs/decision_log.md` 第13段階（風力データ調査）

---

## 1. ペルソナと問い（実装前に固定する。2026-09-20 ユーザー確定）

### 1-1. ペルソナは既存の3モードをそのまま引き継ぐ

風力は**新しいペルソナを作らない**。既存の3つの立場に「オフサイト供給が1つ増える」だけである。

| 需要モード / 事業モード | 使う人 | 風力は何に見えるか | 風力PPA支払は誰のコストか |
|---|---|---|---|
| 産業用・MGタブ ＋ 自家消費型 | 太陽光・蓄電池の**営業担当／EPC**（提案する側） | 需要家に追加提案する電源 | **需要家**のコスト。需要家メリットから引く |
| データセンタータブ | **DC事業者・需要家自身**（設計書 §1 ペルソナ a/b） | 自社で調達する電源 | **自分**のコスト。電気代と並べて見る |
| マイクログリッド（モードB） | **MG事業者**（特定送配電の企画担当） | 網内に供給する電源が1つ増える | **MG事業者**のコスト。運営費に乗り、P-IRRに効く |

**MG事業者から見るとどうなるか（ユーザーからの問い）:**
MGの計算は元々「PV余剰 → 網内需要家に小売価格で売る」構造である。風力はここに供給が足されるだけで、
**計算ロジックは一切変わらない**。変わるのは金の流れの向きだけ。

```
自家消費型・DC:  年間メリット = 電気代削減 + 売電収入 − 風力PPA支払
                 （電気代削減は受電点の基準。届いた風力の託送・賦課金・手数料も引いた値。§5-4）
MG（モードB）:   年間収益    = 網内売電収入 + 系統売電収入 + 束ねメリット
                 年間費用    = 運営コスト + 風力PPA支払 + 届いた風力の託送等   ← ここに入る（W2b）
                 → P-IRR に反映
```

**計算ロジックは変わらない、は W2 の時点の見立てで不十分だった**（風力は送配電網で届くので、敷地内の電源と違い託送等がかかる。
W2c で判明。MGでも同じ扱いが要る）。

MG事業者にとっての意味は「網内の需要家に **24/7に近い電気**を売れる」こと。
テナント誘致の材料になるので、時間一致率はMGモードでも主要な出力にする。

### 1-2. 答えるべき一番の問い

> **24/7（時間単位のカーボンフリー）に近づけるには、何をどれだけ積めばよいか。**

太陽光だけだと、年間電力量で需要の100%を超えても**時間単位の一致率は頭打ち**になる。
このギャップを埋めるのに風力と蓄電池がどれだけ要るかを可視化する。
電気代・CO2・投資回収は**従来どおり出すが、主役ではない**。

主指標:

```
量ベース達成率 [%] = min(1, 年間発電量 / 年間需要量) × 100     ← 従来の「再エネ100%」
時間一致率     [%] = (1 − 年間系統購入量 / 年間需要量) × 100   ← 24/7 の実力
```

この2つの**差**を画面の目立つ場所に出す。蓄電池の放電分は `sc_result["annual_import"]` に
自動で反映されるので、実装コストはほぼゼロ。

### 1-3. 対象範囲（確定）

- **風力のエリアは 北海道(01)・東北(02) の2つだけ。**
- **需要地（地点）も道内・東北に限る。** 風力ONのとき、それ以外の地点は選べない／エラーにする。
  越境託送の料金と制約はモデル化しないため、そもそも表現できない
- 需要モードは**産業用・MG / データセンターの両方**。風力の入力は共通セクションに置くので、
  どちらのタブでも同じように効く（実装は1か所）

該当する `radiation.db` の地点（**8地点**。東北エリアは「東北6県＋新潟県」なので新潟を含む。
当初の設計書は7地点としていたが、W1で東北電力NWの供給区域を確認して修正した）:

| エリア | point_no | point_name |
|---|---|---|
| 01 北海道 | 14163 | SAPPORO |
| 02 東北 | 31312 | AOMORI |
| 02 東北 | 32402 | AKITA |
| 02 東北 | 33431 | MORIOKA |
| 02 東北 | 34392 | SENDAI |
| 02 東北 | 35426 | YAMAGATA |
| 02 東北 | 36126 | FUKUSHIMA |
| 02 東北 | 54232 | NIIGATA |

---

## 2. できるか（結論: できる。侵襲は小さい）

- 下流（自家消費・ルールベース蓄電池・LP最適化・受電上限診断・料金計算・経済性）は
  すべて `(365, 48)` の**発電量配列を1本受け取るだけ**。風力は配列を足せば全機能に波及する
- LP（`optimize_battery`）は発電量を**パラメータ**として扱うので、定式化の変更が要らない
- 需要側で同じことを既にやっている（Phase 7 のDCモード）。`demand_source` / `dc_args` を
  末尾のキーワード引数で足して既存パスをビット同一に保った手口を、そのまま発電側に適用する

---

## 3. データ方針

### 3-1. 水準と形状の分離（DCの需要モデルと同じ構造）

```
風力発電(t) [kW] = min( 契約容量[kW] × 設備利用率 × 形状(t) ,  契約容量[kW] )
                        └ ユーザー     └ 既定29.1%   └ エリア実績（年平均=1.0）
```

| 要素 | 出どころ | 既定値 |
|---|---|---|
| 契約容量 [kW] | ユーザー入力 | なし |
| 設備利用率 [%] | 資源エネルギー庁 調達価格等算定委員会 第112回（2026年1月）「風力発電について」<br>陸上風力（新設）の想定値。定期報告の実績（2022〜2024年設置の平均値の平均 30.7%、中央値の平均 29.5%）とも整合 | **29.1%** |
| 形状(t) | 一般送配電事業者「エリア需給実績」30分値<br>`風力発電実績 ＋ 風力出力制御量`（＝**抑制前**）を年平均1.0に正規化 | 暦年2025 固定 |
| PPA単価 [円/kWh] | 同委員会 第112回「今年度入札における平均落札価格は11.96円/kWh、上限価格13.00円/kWh」 | **11.96 円/kWh** |

**PPA単価の出典について（2026-09-20 ユーザー提供資料を確認）:** 陸上風力の**公的な価格**は、いずれも
FIT/FIP（国の支援制度）の価格で、**オフサイトPPAの相対契約の単価そのものではない**。PPAの単価に公的な出典はなく、
公的に確認できる中で「実際に事業が成立した水準」に最も近いのが入札の平均落札価格である。

| 資料 | 値 | 意味 |
|---|---|---|
| 調達価格等算定委員会 第112回（2026年1月）「風力発電について」 | 平均落札 **11.96円/kWh**（上限 13.00円） | 2025年度入札（最新）。**既定値に採用** |
| 同「令和7年度以降の調達価格等に関する意見」（2025年2月3日。ユーザー提供） | 平均落札 12.73円/kWh（上限 14.00円） | 2024年度入札（1年前） |
| 同上 結論（p.121〜123）の陸上風力（新設・50kW以上）の入札上限額 | 2024年度 14円 → 2025年度 13円 → 2026年度 12円 → 2027年度 11.8円 | FIP基準価格の上限（入札）。2026・2027年度は第112回で改めて設定し直しとされた |
| 同上 陸上風力（新設・50kW未満）のFIT調達価格 | 14円 → 13円 → 12円 → 11.8円（2024〜2027年度） | 50kW未満。地域活用要件（自家消費・地域消費・地域一体型）を満たすものだけがFIT対象（注７） |
| 同上 陸上風力（リプレース）のFIT/FIP | 12円/kWh（2025年度） | 1,000kW以上はFIPのみ（注８） |
| 同上 想定値（陸上風力・新設） | 資本費27.1万円/kW、運転維持費0.85万円/kW/年、設備利用率29.1%、IRR 6%（2027年度は5%）、運転年数20年（2027年度は25年） | 設備利用率29.1%の出典。**自己所有の風力（v2）の資本費・運転維持費もここが出典** |

より新しい年度の入札結果（11.96円）を既定に置く。単価は入力欄で上書きできる。

**抑制前を使う理由:** 系統都合の出力制御は売る側のリスクであって、風況（資源量）ではない。
買い手への提案では資源量を基準にし、抑制率は別途情報として表示する。

### 3-2. 採らなかったもの（理由は decision_log 第13段階）

- **METPV-20 の風速（要素7）**: 日射との日内相関が11地点平均で **+0.78**。
  ハブ高への外挿指数 α を 0.10〜0.30 で振ると設備利用率が **7.2%〜21.9%** と3倍動き、
  実際の陸上CF（20〜25%）がその帯の内側に入る。**仮定が答えを作ってしまう**ため使えない
- **windpowerlib 等のOSSエンジン**: 風速時系列→パワーカーブ の経路を採らないので**依存に追加しない**。
  「UIで直感的に、データを用意せずに検証できる」というテーマにも合わない
- **renewables.ninja**: CC BY-**NC**。営業ツールに使えない
- **REPOS（環境省 風況マップ、地上80m・500mメッシュ・20年平均）**: 年平均風速のみで時系列がなく、
  風速→CF の変換に結局パワーカーブとワイブル分布の仮定が要る。地点別CF補正の候補として将来に残す

### 3-3. データ取得の実地検証（2026-09-20 実施済み・東北 暦年2025）

```
行数 17,520（= 365日 × 48コマ。非うるう年なのでMETPVの月日軸とそのまま合う）
平均 605.3 MW / 最大 2,019.0 MW / 最小 1.0 MW
出力制御 72.49 GWh（発電量の 1.37%）
形状の最大値 3.336
  → CF 24.6% ならピーク 0.821（クリップなし）
  → CF 29.1% ならピーク 0.971（クリップなし）
  → CF 35.8% ならピーク 1.194（0.7% がクリップされる）
日内（正規化）: 0時1.02 4時1.01 8時0.94 12時1.04 16時1.02 20時0.98   ← ほぼ平坦。夜も出る
月別（正規化）: 1月1.19 2月1.83 3月1.35 4月1.13 5月0.78 6月0.60
                7月0.41 8月0.49 9月0.61 10月0.66 11月1.35 12月1.68   ← 冬に強く夏に弱い（4.5倍）
```

太陽光と**季節が真逆**、**日内は平坦**。これが併用の価値の源泉である。

URL（`eria_jukyu_YYYYMM_NN.csv`。暦年2025の1月・12月とも取得成功を確認済み）:

| コード | エリア | ベースURL |
|---|---|---|
| 01 | 北海道 | `https://www.hepco.co.jp/network/con_service/public_document/supply_demand_results/csv/` |
| 02 | 東北 | `https://setsuden.nw.tohoku-epco.co.jp/common/demand/` |

#### 取得時の落とし穴（実測で判明。実装者は必ず読むこと）

1. **User-Agent が要る。** 付けないと 403 で弾かれる
2. **文字コードが統一されていない。** 北海道・東北は cp932（他エリアにはUTF-8のものもある）。
   `cp932 → utf-8` の順でデコードを試す
3. **列は必ず列名で拾う。** エリアによって列数・並びが違う（中国電力には `火力出力制御量` がある等）
4. **ヘッダ行は先頭行ではない。** `DATE` を含む行を探してそこから読む
5. 単位は `[MW平均]` ＝ **30分間の平均電力**。電力量にするには × 0.5h
6. **北海道は月の日数が少ない月を、カンマだけの空行で31日分（1488行）に埋めている。**
   行数を数えるとうるう年でもないのに 372日分（17,856行）になる。日付が空の行は詰め物なので捨てる。
   日付があるのに値が空の行は本物の欠測なのでエラーで止める（W0で実装済み）

### 3-4. 北海道の実測値（W0で確認。2026-09-20）

```
平均 356.1 MW / 最大 974.0 MW / 形状の最大値 2.735 / 出力制御 0.07%
CF 29.1% ならピーク 0.796（クリップなし）
日内（正規化）: 0時1.04 4時1.02 8時0.96 12時0.97 16時0.96 20時1.00       ← 東北同様ほぼ平坦
月別（正規化）: 1月1.04 2月1.27 3月1.14 4月0.98 5月0.94 6月0.90
                7月0.82 8月0.53 9月0.77 10月0.82 11月1.33 12月1.49       ← 冬>夏だが東北より穏やか（2.8倍）
5MW未満のコマ 101（0.6%）、同値が続く最長 7コマ → 凪の時間帯で、値の張り付きではない
```

東北（冬夏差 4.5倍・出力制御 1.37%）と北海道（冬夏差 2.8倍・出力制御 0.07%）は**性格が違う**。
エリアを選ばせる意味がある。

---

## 4. 前提（2026-09-20 ユーザー承認済み）

| # | 論点 | 本設計の案 |
|---|---|---|
| 1 | オフサイト風力の電気はどこに届くか | **受電点に届く**。**【W2c で改訂】** 当初は「太陽光と同じ扱いで買電を減らし、契約電力も下げうる／託送料はPPA単価に含める」としたが**誤り**だった。届いた電気にも託送の従量分・再エネ賦課金・小売手数料がかかり、契約電力（基本料金）は下がらない（§5-4）。託送の従量分Xと小売手数料Yを入力に持つ |
| 2 | 支払の対象 | **pay-as-produced（全発電量に単価を掛ける）**。余剰・抑制になった分も支払う。結果に「届いて使えた分／無駄になった分」を出して無駄が見えるようにする（W2c で按分から実測の配達量に変更） |
| 3 | FIT売電との併用 | **【W2c で改訂】** 売電・出力抑制の対象は敷地内の太陽光の余剰だけ（風力の余剰は売電できず無駄になる扱い）。よって風力の余剰にFITが付く問題は起きない（W2で出していたFIT注記は不要になった） |
| 4 | 太陽光と風力の同時性 | **METPV-20は平年値、風力は2025年実績で、別のデータ。** 日単位・コマ単位の同時性は担保されない。<br>意味があるのは**月別・時間帯別の平均的な補完関係**まで。画面と文書の両方に明記する |

**4 は本機能の最大の限界。** 「凪と曇天が重なる日」のような評価には使えず、蓄電池のサイジングも
この前提の上での参考値になる。24/7一致率も同じ制約を負う。

---

## 5. 実装設計

### 5-1. データファイル

| ファイル | 内容 |
|---|---|
| `tools/build_wind_shape.py` | **開発用スクリプト（実行時には不要）**。エリア需給実績CSVを取得し、正規化形状を書き出す |
| `wind_shape.csv` | 17,520行 × 2エリア列（`area_01`, `area_02`）。値は `(実績+制御量)/年平均` を**小数6桁**（約450KB）。<br>先頭3列は `month,day,slot`（slotは0-47）で検証用。丸めのため年平均は 1.0 から 1e-8 ほどずれるので、**`load_wind_shape` が読み込み時に厳密に 1.0 へ再正規化する** |

`app.py` のスタンドアロン方針は維持（データファイルの同梱は `comstock_*.csv` と同じ扱い）。
2エリアで約450KB（実測）。

エリアのメタ情報は `app.py` の定数に出典コメント付きで持つ。

```python
# 出典: 各一般送配電事業者「エリア需給実績」暦年2025（30分値）。tools/build_wind_shape.py で集計
WIND_AREA_META = {
    "02": {"name": "東北", "mean_mw": 605.3, "max_mw": 2019.0,
           "curtail_pct": 1.37, "shape_max": 3.336},
    "01": {"name": "北海道", "mean_mw": 356.1, "max_mw": 974.0,
           "curtail_pct": 0.07, "shape_max": 2.735},
}
```

### 5-2. 計算層（新規関数。`calculate_generation` は触らない）

```python
def load_wind_shape(area_code):
    """wind_shape.csv から (365, 48) の正規化形状を返す。年平均=1.0"""

def resolve_wind_capacity(sizing_mode, capacity_kw=None, coverage_pct=None,
                          annual_demand_kwh=None, cf_pct=29.1):
    """容量の解決。カバー率指定なら capacity = 年間需要 × 率 / (8760 × CF)"""

def build_wind_30min(area_code, capacity_kw, cf_pct=29.1):
    """(365,48) kWh/30分 を返す。定格でクリップし、クリップ量も返す
       returns: {"gen_30min", "annual_kwh", "clipped_kwh",
                 "effective_cf_pct", "area_code", "capacity_kw"}
    """

def station_to_wind_area(point_no):
    """地点 → エリアコード（'01'/'02'/None）。明示的な辞書で持つ（緯度経度から推定しない）"""
```

### 5-3. `run_simulation` への統合

シグネチャ末尾に**キーワード引数を2つ**追加（DCモードと同じ手口。省略時は従来どおり）。

```python
def run_simulation(..., demand_source=DEMAND_SOURCE_INDUSTRIAL, dc_args=None,
                   pv_enabled=True, wind_args=None):
```

- `pv_enabled=False` のとき `faces=[]` にする（風力単独運転）
- 既存の「有効な面設定がありません」エラーは **`pv_enabled=True` かつ風力OFF のときだけ**出す
- 太陽光・風力ともOFF → 新しいエラー
- 風力ONで地点が道内・東北でない → エラー（§1-3）
- 合成:
  ```python
  gen_pv    = result["total_gen_clipped"]
  gen_total = gen_pv if wind_info is None else gen_pv + wind_info["gen_30min"]
  ```
  `gen_total` を下流すべて（自家消費・蓄電池・LP・受電上限診断・グラフ）に渡す。
  **風力OFFのときは `gen_total is gen_pv`（同一オブジェクト）**にしてビット同一を保証する
- `result` に `gen_pv` / `gen_wind` / `wind_info` を持たせ、内訳を表示に使う

#### W2 実装メモ（2026-09-20。設計からの差分と、実装で決めたこと）

- **調達エリアは入力に持たない。** 需要地（観測地点）と同じエリアに限るので、エリアは地点から一意に決まる
  （`station_to_wind_area`）。設計書 §5-6 の「調達エリア Dropdown」は入力でなく**表示だけ**にする（W3）。
  `resolve_wind(wind_args, point_no, annual_demand_kwh)` が、地点が対象外・CSVアップロード（地点なし）のときにエラーを返す
- `wind_args` のキーは `WIND_INPUT_KEYS`（`enabled` / `sizing_mode` / `capacity_kw` / `coverage_pct` / `cf_pct` / `ppa_price` /
  `wheeling_yen` / `retail_fee_yen`。後ろの2つは W2c で追加）。`cf_pct`・`ppa_price`・`wheeling_yen`・`retail_fee_yen` は
  省略（None）で出典付きの既定値（29.1% / 11.96円 / エリア×契約種別の表 / 3.0円か1.5円）
- **グラフと `result_state` は合計を見せる。** 風力ありのときだけ `chart_result`（`result` のコピー。`total_gen_clipped` と
  `monthly` を合計に差し替え、内訳は `gen_pv` / `gen_wind` / `wind_info`）を返す。風力なしは `result` そのもの。
  月別・日別グラフの**内訳の積み上げ表示は W3**（今は合計1本）
- **併用できないものは明示エラー**（黙って誤った数字を出さないため）: MG（W2bまで。MGの `pv_revenue` は `annual_self` に風力の自家消費を含んでしまう）、
  系統受電上限（DC）、最適容量探索（この2つはW2c。§5-4）
- **投資回収**: 分母の年間メリットから風力PPA支払を引く。初期投資は PV＋蓄電池＋受電設備のまま（風力を含めない）。
  風力のみ（初期投資0）は「回収年数は該当しません」と出す（風力ONのときだけの分岐。従来の出力は変えない）
- **リース／PPA（モードA）**: リース料は風力の有無で**変わらない**（対象は PV＋蓄電池の設備）。需要家メリットは
  風力PPA支払を引いた値。**PPA単価の分母（自家消費量）は、発電量比で太陽光分に按分する**（自家消費した電力が太陽光由来か
  風力由来かは分けられないため。結果に按分した旨を出す）
- ~~余剰の按分／FIT注記／最適容量探索での支払の控除~~ は **W2c で置き換えた**: 無駄になった風力は実測の配達量から求める
  （按分をやめた）。売電は太陽光の余剰だけなのでFIT注記は不要。最適容量探索は併用不可にした（§5-4）
- **蓄電池LPは電気代の最小化**で動く。24/7の時間一致率は結果として上がる値で、最大化はしていない（結果に注記を出す）
- **24/7の節**（`format_247`）: 蓄電池なしの参考3行（太陽光／風力／合計）＋設定どおり＋月別＋同時性の限界の注記

### 5-4. 経済性への反映（W2c で全面改訂。2026-09-20）

**W2 の誤り（ユーザー指摘で判明）:** W2 は風力を敷地内の太陽光と同じ扱いで購入量から引き、電気料金を再計算していた。
これはオフサイト（送配電網で届く）電源には成り立たない。実測では、風力のみで純メリットが＋65.8万円/年→−9.8万円/年
（基本料金と賦課金の分を外すだけ）に反転し、託送の従量分・小売手数料を引くと−97.6万円/年になった。前提1（託送はPPA単価に含める）は
**節約額の側を正さない**ので不十分だった。

#### オフサイト電源（フィジカルPPA）の費用構造

送配電網で届く電気は、需要家が受電点で系統から受ける。次の費用は**削減されない**（出典: [東北電力NW「料金の仕組み」](https://nw.tohoku-epco.co.jp/consignment/pdf/fee_calculation.pdf)、
[自然エネルギー財団 p.10・19・23](https://www.renewable-ei.org/pdfdownload/activities/REI_JPCorporatePPA_2024.pdf)）。

| 費用 | 敷地内PV | オフサイト風力 |
|---|---|---|
| 託送料金（基本料金 円/kW × 契約電力） | 契約電力が下がれば下がる | **下がらない**（受電点の最大は届いた風力も含む） |
| 託送料金（電力量料金 円/kWh × 使用電力量） | かからない | **届いた分にかかる** |
| 再エネ賦課金 | 削減される | **届いた分にかかる** |
| 小売事業者の手数料（バランシング等） | なし | **届いた分にかかる** |
| PPA単価 | （設備投資から逆算） | **発電した全量に**支払う（pay-as-produced） |

#### 計算（受電点の基準。`offsite_receiving` / `offsite_cost_after`）

```
受電量        R(t) = max(0, 需要 + 充電 − 放電 − 敷地内の太陽光)     ← 契約電力・受電上限の基準
小売から買う量      = 運転結果の系統購入 import_(t)                  （風力で賄えなかった残り）
風力の配達量  D(t) = clip(R − import_, 0, 風力発電量)
敷地内PVの余剰(t)  = max(0, PV + 放電 − 需要 − 充電)                 ← 売電・出力抑制の対象はこれだけ
無駄になった風力   = 風力発電量 − 配達量                              （売電できない）

導入後の電気代 = 基本料金(R の最大)                                   ← 風力では下がらない
               + 電力量料金(小売から買う量)                           ← 従来どおり
               + Σ D × (託送の電力量料金 X + 再エネ賦課金 + 小売手数料 Y)
風力PPA支払    = 風力の年間発電量 × PPA単価
年間経済メリット = 電気代削減 + PVの余剰の売電収入 − 風力PPA支払
```

**蓄電池の運転（収支）は太陽光＋風力を合わせた発電のまま**（風力が余る時間に蓄電池が充電し、凪の時間に放電する効果＝24/7の主役を残すため）。
料金だけを受電点の基準で計算し直す。収支の恒等式（残差 ≤ 5e-6 kWh。LPの数値誤差）と、
「太陽光の余剰 ＋ 無駄になった風力 ＝ 運転結果の余剰」は、ルールベース・LPともテストで確認している。

#### 入力と既定値（出典のあるものだけ）

| 入力 | 既定 | 出典 |
|---|---|---|
| X: 託送の電力量料金 [円/kWh] | 北海道 高圧2.28／特高1.02、東北 高圧2.15／特高0.97 | 北海道電力NW「託送供給の料金単価」、東北電力NW「託送供給等約款の変更届出」（2025-07-29、届出単価）。標準接続送電、2025年10月〜、**消費税等相当額を含む表示のまま** |
| Y: 小売手数料 [円/kWh] | 高圧3.0／特高1.5 | 自然エネルギー財団の**推定**（2023年度・全国平均、フィジカルPPAの「小売コスト」）。公的な料金表はなく唯一の出典。画面に「推定値」と明記する |

- ⚠ **検索要約の「東北 高圧4.40円/kWh」は誤りだった。** 一次資料（届出のPDF）は2.15円/kWh。二次情報は必ず一次資料で確認すること
- 既存の電気料金欄（東京電力EP）の税区分は未確認のため、Xの税込表示は補正していない（差は約0.2円/kWh）
- 電気料金欄の既定は東京電力EPのまま（既存の制限）。小売単価に含まれる託送分と、Xを引くエリアが食い違う（例: 仙台の需要家にTEPCOの単価）。単価は入力で上書きできる
- 発電側課金（系統連系受電サービス料金）は発電事業者の負担で、PPA単価に含まれる。需要家の入力は不要

#### 併用できないもの（明示エラー）

蓄電池LP・最適容量探索は、風力を敷地内の発電と同じに扱って最適化する。受電点基準の制約や経済性は、その前提では正しく評価できない（MGは W2b で対応済み）:

- **系統受電上限（DC）**: 上限は受電量 R に掛かるが、LPは小売購入にしか制約を掛けない → 併用は明示エラー
- **最適容量探索**: 探索内の経済性が受電点基準でない → 併用は明示エラー
- 蓄電池LPの「最適化年間コスト・ピーク」は風力併用では表示しない（受電点基準の料金は別に計算し直している）

これらを解くには、LPに「風力の配達量」の変数を足す（W2d。LPが1.5倍程度に大きくなる見込み）。

| モード | 反映先 |
|---|---|
| 自家消費型（産業用）・DC | 上記の年間経済メリット。初期投資は従来どおり（PV+蓄電池+受電設備）。風力は含めない |
| モードA リース/PPA逆算 | 投資ベースは従来どおり（リース料は風力の有無で変わらない）。**需要家メリットは風力込み（PPA支払・託送等を引いた）値**。PPA単価の分母は、自家消費量を発電量比で太陽光分に按分 |
| モードB MG | **W2b で実装済み。** 収益 ＝ 網内に供給した全量（敷地内PV・蓄電池分＋風力の配達量）× 網内単価 ＋ 束ねメリット。費用 ＝ 運営コスト ＋ 風力の調達費用（PPA支払＋届いた分の託送・賦課金・手数料）。束ねメリットは風力では増えない（契約電力が下がらない）。**PPA（MG）の単価は（投資の回収 ＋ 風力の調達費用）÷ 網内に供給した全量で逆算**（P-IRR ≒ 目標IRR ＋ 束ねメリット、の性質を保つ）。需要家メリットは 風力込みの年間メリット − 投資の回収分（リースもPPAも同じ値になる） |

自己所有の風力（資本費 27.1万円/kW・運転維持費 0.85万円/kW/年 — 同委員会の想定値）は**v1では作らない**。
既存の経済性にPVのO&Mの概念が無く、風力だけ運転維持費を入れると非対称になるため（v2候補）。

#### 将来: オフサイト太陽光（ユーザーの問い 2026-09-20）

電源の種類（太陽光／風力）と、届き方（敷地内／オフサイト）は別の軸である。W2c の経済性は**オフサイト電源のリスト**を
受け取る形（`offsite_receiving` / `offsite_cost_after` は複数電源に対応済み）で、風力はその1件目。
オフサイト太陽光は、発電所の場所（別の観測地点）の日射で発電量を作って同じリストに加えればよい（W6候補）。
制約: 託送の従量分Xを用意できたエリア（北海道・東北）に限る。UIとMCPに出すのは風力だけ。

### 5-5. 24/7（時間一致率）— 本機能の主役

結果テキストに専用の節を作る。

- **量ベース達成率** と **時間一致率** の両方（この差が24/7の本質）
- 月別の時間一致率の表
- 太陽光だけ / 風力だけ / 合計 の年間発電量の内訳
- 風力の実効設備利用率・クリップ量・エリアの抑制率

### 5-6. UI

- 「⚙️ 太陽光発電設定」の面設定の先頭に **`☑ 太陽光発電を使う`（既定ON）**
- その直後に新アコーディオン **「🌀 風力発電設定（オフサイトPPA）」**（共通セクションに置く＝両タブで効く）

| 入力 | 種類 | 既定 |
|---|---|---|
| 風力発電を併用する | Checkbox | OFF |
| 調達エリア | **表示のみ（入力しない）** | 需要地の地点から決まる（越境しないため） |
| 容量の指定方法 | Radio「容量を指定」／「需要カバー率で指定」 | 容量を指定 |
| 契約容量 [kW] | Number | なし |
| 需要カバー率 [%] | Number | なし |
| 設備利用率 [%] | Number | 29.1 |
| PPA単価 [円/kWh] | Number | 11.96 |
| 託送の電力量料金 [円/kWh] | Number | エリア×契約種別の表（§5-4。北海道 高圧2.28、東北 高圧2.15など。空欄で既定） |
| 小売手数料 [円/kWh] | Number | 3.0（高圧）／1.5（特高）。**推定値**と画面に明記。空欄で既定 |

- 既定値の直下に出典（調達価格等算定委員会 第112回）を短く表示する
- §4-4 の同時性の制約を画面にも出す
- グラフ: 月別グラフに風力を積み上げ、日別48コマグラフに風力のトレースを追加

#### W3 実装メモ（2026-09-21）

- 入力は `on_click` の**末尾に追加**（既存の並び 48＋18＋40… は不変）。末尾 = 太陽光を使う(1) ＋ 風力 `WIND_INPUT_KEYS` 8欄。`build_wind_args` が UI の値を `wind_args` にする。OFFなら風力欄は検証しない（不正な値が残っていても計算できる）
- **任意入力（契約容量・カバー率・託送・手数料）は空欄＝既定値のテキスト欄。** `gr.Number` は空欄を表せず未操作でも0を送る（方位角欄と同じ既知の仕様）ため。設備利用率とPPA単価は出典のある既定値を入れた Number
- **調達エリアは入力でなく表示**（`wind_area_note`）。地点・CSV・契約種別の変更に追従し、既定の託送・手数料もここに出す
- グラフ: 風力ありのときだけ別関数（`_make_monthly_chart_wind`）で月別を「太陽光＋風力の積み上げ」に、日別は合計に「うち太陽光」「うち風力（発電量）」の線を足す。**風力なしの図は従来の関数のままでバイト同一**
- 需要が未設定（施設タイプ「なし」）で風力ONは、需要の設定を促すエラー（UIの既定は施設なしなので、初めて風力を試す人が最初に踏む）
- 検証: 「計算」ボタンの関数を、UIの入力コンポーネントの並びどおりに呼ぶテスト（各欄に別々の値を入れて正しい欄に届くかを見る）。実ブラウザ（内蔵ブラウザ）でも、風力ONの設定欄の表示・エリア表示・エラー表示・データセンターでの計算完了を確認、コンソール・サーバーのエラーなし。図の見た目のスクリーンショットは取れなかった（ペインが黒画面）ので、**グラフの見た目はユーザー確認が必要**

### 5-7. MCP

- `list_wind_areas()` — 新規。エリア一覧・母数・抑制率・月別形状を返す
- `estimate_wind_generation(area, capacity_kw, cf_pct)` — 新規。`estimate_pv_generation` と対になる
- `simulate_industrial_pv` / `simulate_dc` に**任意引数 `wind`（dict）と `pv_enabled`** を追加

#### W4 実装メモ（2026-09-21）

- **`wind` は末尾の任意引数**（`validate_industrial_params` / `simulate_industrial_pv` / `validate_dc_params` / `simulate_dc`）。省略時の出力は従来とバイト同一（12ケースで確認。`assumptions` にも `wind` / `pv_enabled` のキーを足さない）。書式は {"capacity_kw": 1000} か {"coverage_pct": 100}（どちらか1つ）＋任意の `cf_pct` / `ppa_price_yen_per_kwh` / `wheeling_yen_per_kwh` / `retail_fee_yen_per_kwh`。**既定値は検証時に解決して `normalized_params.wind` に入れる**（エージェントがユーザーに確認できるように）。未知のキー・両方指定・どちらもなし・範囲外・対象外の地点はすべて入力エラー
- **MCPの計算は `run_simulation` を呼ばない**（`_run_industrial_simulation` が同じ計算を独立に持つ）ため、風力の経済性はUIと同じ関数（`resolve_wind` / `offsite_receiving` / `offsite_cost_after`）を呼んで移した。二重実装は避けた。UIとの数値一致をテストで確認（メリット・配達量・費用・契約電力・24/7・MGのP-IRR・PPA単価・LP・DC）
- **`pv_enabled=false`** のときは `faces` を検証せず（空でもよい）、`wind` が必須。MCPでは面のPpeak=0は入力エラー（UIと違い、風力のみにしたいときは `pv_enabled=false` と明示する）
- **併用できないもの**: DCの系統受電上限（`validate_dc_params` が入力エラーにする）。MGは併用できる
- 結果に **`wind` 節**（発電・届いた量・無駄になった量・費用の内訳・24/7の一致率・月別）と、`annual.pv_generation_kwh` / `total_generation_kwh`、`electricity_cost.annual_wind_ppa_payment_yen` を足す。MGは `wind_procurement_cost_yen_per_year`。風力ありの `caveats` に、オフサイトの費用構造・同時性の限界・託送の税区分と手数料の推定・PPA単価の意味を入れた
- **新ツール2つ**: `list_wind_areas`（エリア・地点・月別／時間帯別の形状・託送の既定値・出典）、`estimate_wind_generation`（`station_no` / `capacity_kw` / `cf_pct` → 年間・月別発電量。`estimate_pv_generation` の風力版）
- **プロトコル層の確認（2026-09-21、サーバー起動＋MCPクライアント）**: `tools/list` は9ツール、`simulate_*` のスキーマに `wind` / `pv_enabled`。`tools/call` の結果は数値が数値のまま（`int` / `float`）で、引用符付きの数値文字列はなし。**Gradio のMCPは結果を Python リテラル表記の文字列で返す**（`{'a': 1}`）ので、クライアントは `json.loads` でなく `ast.literal_eval` で読む。`simulate_*` は docstring に `Args:` 節がなくスキーマの引数説明が空のため、ツール説明の段落に `wind` の書式を書いた
- **成功系の `return` は `_jsonable()` で包む**（プロジェクト規約）。変更後は
  プロトコル層（`tools/call`）で型まで確認する（numpy型が文字列になる既知の罠）

---

## 6. 作業段階（Sonnet向け）

各段階の終わりに**既存テストを全部回す**。PASS数が減ったら止めて報告すること。
既存: `test_fable5_fixes`(24) / `test_mcp_tools`(45) / `test_verify_battery_bug` /
`test_dc_mode`(48) / `test_azimuth_input`(19) / `test_grid_cap`(75) / `test_mcp_dc_tools`(126)

| 段階 | 内容 | 完了条件 |
|---|---|---|
| **W0** ✅ | `tools/build_wind_shape.py` を書き、暦年2025の北海道・東北を取得して `wind_shape.csv` を生成。`WIND_AREA_META` と既定値（設備利用率29.1%・PPA単価11.96円）を `app.py` に置く | 各エリア17,520行・年平均1.0（CSV上は誤差1e-6以内、読み込み時に厳密化）・METPVの月日軸と一致。**東北の検証値（平均605.3MW / 最大2,019.0MW / 形状最大3.336 / 抑制1.37%）が再現**。`test_wind_shape.py` PASS 24 |
| **W1** ✅ | 計算層（§5-2）＋ 地点→エリア対応表 | `test_wind_shape.py` PASS 86（W0の24 + W1の62）。8地点の対応、対象外はNone。CF35.8%でクリップが効き量が報告される。**実装を壊すと落ちることを変異テストで確認済み** |
| **W2** ✅ | `run_simulation` 統合（`pv_enabled` / `wind_args`、発電合成、24/7指標、自家消費型・DCの経済性） | **風力OFF時に既存の出力が完全一致**（10シナリオ×7項目=70項目のバイト一致。産業用・MG・DC・両面・最適容量探索を含む）。`test_wind_mode.py` |
| **W2c** ✅ | 経済性を受電点基準に作り直し（託送の従量分・賦課金・小売手数料・契約電力は下がらない・売電は太陽光の余剰のみ）。入力X・Yを追加。受電上限・最適容量探索との併用は明示エラー | 別経路で再計算した値と一致（`test_wind_mode.py` PASS 105）。風力OFFは70項目バイト一致のまま。**変異テストで検出を確認** |
| **W2b** ✅ | MG（モードB）への反映（風力の調達費用を年間費用に、PPA単価の逆算に、P-IRR・CFに反映） | 受電点基準で独立に再計算した収益・費用・P-IRRと一致（`test_wind_mode.py` PASS 122）。風力OFFは70項目バイト一致のまま。**変異テストで検出を確認** |
| **W2d**（候補） | LPに「風力の配達量」変数を足し、受電上限・最適容量探索・LPの目的関数を受電点基準にする | 受電上限を守る。LPの実行時間が許容内 |
| **W3** ✅ | UI（チェックボックス・アコーディオン・グラフ・注記） | `test_wind_ui.py` PASS 46（UI配線・変換・表示切替・グラフの内訳）。変異10件を検出。風力OFFは70項目バイト一致。ブラウザで動作確認済み。**Firefoxでのユーザー確認は未** |
| **W4** ✅ | MCPツール2本追加（`list_wind_areas` / `estimate_wind_generation`）＋`simulate_*` / `validate_*` に `wind`・`pv_enabled` | `test_mcp_wind_tools.py` PASS 74（UIと数値一致・入力検証・型）。省略時は従来の出力とバイト同一（12ケース）。変異15件を検出。プロトコル層で型確認済み。**Codex・Claude Code での実機検証は未** |
| **W5** | 文書（`design_spec.md` に風力の節、`decision_log.md` に判断、`CLAUDE.md` の要約とテスト一覧、`architecture.md`） | 設計の正典が docs/ に揃う |
| **W6**（候補） | オフサイト太陽光（発電所の場所の日射で発電量を作り、オフサイト電源のリストに加える） | §5-4「将来」。Xを用意できたエリア内に限る |

### 厳守事項（既存の規約から）

- **既存の産業用・MG・DCの機能は一切削除しない。** 風力は発電源の追加であって置き換えではない
- **push はユーザーの明示的指示を待つ**（`origin` = HF Space なので `main` への push = 本番デプロイ）
- **根拠のない数値を既定値に埋めない。** 本書の既定値（29.1% / 11.96円）は出典付き。
  出典が無いものはユーザー入力にして既定を置かない
- 個人情報を含むコードをコミットしない

---

## 7. v1 に入れないもの（意図的に落とす）

| 項目 | 理由 |
|---|---|
| 北海道・東北以外のエリア | ユーザー判断（2026-09-20）。越境託送をモデル化しないため需要地も道内・東北に限る |
| 風速→パワーカーブ（windpowerlib 等） | 外挿の仮定が答えを作る。テーマ（データ準備不要）にも反する |
| 自己所有の風力（資本費・運転維持費） | 既存の経済性にPVのO&Mが無く非対称になる。v2 |
| REPOS による地点別CF補正 | shapefile の同梱が要る。v2 |
| 洋上風力 | 形状がエリア実績に混ざっており分離できない |
| 託送料の別建て | 単価の出典が無い |
| 形状年の選択・複数年平均 | UIを増やす割に得るものが小さい。まず1年で運用する |
| 「必要な風力容量を自動で逆算」 | まず手で振って傾向を見る。自動探索は蓄電池の最適容量探索と同じ枠組みで後から足せる |
